feat(knowledge_base): implement offline research memory and analyst workspace

- Implemented `KnowledgeDocumentDiscovery` for scanning files without active execution.
- Added text extraction with sensitive data masking and token estimation.
- Integrated NLP chunking for structured index generation.
- Created `KnowledgeIndex` enabling search via symbol, module, and keywords.
- Built TF-IDF and simple Fuzzy retrieval algorithms, merging them via `HybridRetrieval`.
- Designed memory cards (Symbol, Hypothesis, Governance, Warning, Planning).
- Added Decision Journal mechanism for offline tracking without live execution implications.
- Developed `ResearchQueryEngine` for extracting QA with source mapping.
- Handled reporting (`FindingsDigest`, `WorkspaceSummary`, `KBQuality`) providing strict local disclaimers ensuring no live-trade conflations.
- Handled settings, paths, datalake saving, featurestore loading, and required reporting tests.
