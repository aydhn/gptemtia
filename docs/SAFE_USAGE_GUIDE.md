# GLOBAL PROJECT POLICY (2026-09-17)
> **ÖNEMLİ KURAL GÜNCELLEMESİ**: Ayrıntılar için `GLOBAL_PROJECT_POLICY.md` belgesini inceleyiniz.
> - **Kesin Yasak**: Gerçek borsa/broker bağlantısı, canlı emir gönderimi, gerçek para ile canlı işlem, broker API key/secret, production deployment, yatırım tavsiyesi, kâr garantisi, web scraping, lookahead bias.
> - **İzinli ve Hedeflenen**: Gerçek tarihsel veriyle local backtest, local paper trading motoru (sanal bakiye/pozisyon/emir/fill/PnL), Telegram research/paper-trade sinyalleri (PAPER BUY/SELL/EXIT/WATCH/HOLD), model confidence/risk/regime açıklamaları, walk-forward/OOS/slippage testleri, local ML training/inference, local optimizasyon.
> - **Geçmiş İfadeler**: Önceki fazlardaki "disabled" ifadeleri yalnızca canlı broker/trade/production bağlamında geçerlidir; yerel araştırma, model eğitimi, backtest ve Telegram paper trading'i engellemez.

## Local Continuity Intelligence
- Operator memory book nasıl okunur? -> data/lake/local_continuity_intelligence ve reports altından.
- Lessons-learned codex nasıl yorumlanır? -> Bu sadece local bir prova olup official report değildir.
- Decision rationale capsule neden official decision record değildir? -> Sistem hiçbir recordı official olarak sunamaz, yatırım tavsiyesi veremez, cloud memory kullanamaz.
- Future-reader guide nasıl kullanılır? -> Onboarding map ve reading route ile projeye hizli giriş.
- Continuity binder nasıl okunur? -> Tüm bilgilerin indekslenmiş özetidir.
- Command/output interpretation guides neden komut çalıştırmaz? -> Bu fazda no-go durumundadır ve safe execution kuralları gereği komutlar otomatize edilmez.
- Gerçek memory system, cloud sync, official decision record, package publish, deployment, canlı emir, broker execution ve yatırım tavsiyesi YOKTUR.

## Local Documentation Export
- **Static site export rehearsal nasıl okunur?** Bu sadece dosya üretimidir, gerçek web server çalıştırmaz.
- **Offline HTML pack neden dashboard/deploy değildir?** Çünkü statiktir ve JS/telemetry barındırmaz.
- **Printable binder nasıl yorumlanır?** Yazdırılabilir bölümlerin index'idir, PDF binary oluşturmaz.
- **PDF-ready layer neden PDF binary üretmez?** Çünkü harici bağımlılıklardan kaçınır, sadece markdown/html oluşturur.
- **Presentation-freeze neden PowerPoint/slides değildir?** PPTX oluşturmak yerine text formatında arşiv narrative sunar.
- **Source/output/route maps nasıl okunur?** Yalnızca lokal rehberdir.
- **Gerçek web server, PDF export, slides generation, cloud hosting, package publish, deployment, canlı emir, broker execution ve yatırım tavsiyesi OLMADIĞINI UNUTMAYIN.**
\n
## Local Reproducibility Governance
- Reproducibility dossier nasıl okunur? -> Dossier, sistemin nasıl yeniden üretilebileceğini gösterir ancak attestation değildir.
- Environment replay manifest neden install/provisioning talimatı değildir? -> Çünkü sadece offline replay sağlar, kurulum yapmaz.
- Deterministic runbook neden determinism guarantee değildir? -> Deterministik çalışmayı sadece hedefler, garanti etmez.
- Build-free reproduction layer nasıl yorumlanır? -> Gerçek bir build işlemi yapılmadan sistemin okuma katmanıdır.
- Drift/variance/integrity çıktıları nasıl okunur? -> Sadece veri bütünlüğü ve sapmasını gösterir.
- Terminal reproducibility governance binder nasıl yorumlanır? -> Gerçek build, Docker image, CI/CD, dependency install, package publish, deployment, canlı emir, broker execution ve yatırım tavsiyesi olmadığı açık yazılsın.
\n

# Phase 98 Updates
Local completion governance framework appended.


> **UYARI**: Bu proje ve modülleri offline/local terminal closeout ve pre-final handover governance rehearsal çıktısıdır; gerçek project closeout, official governance seal, official acceptance, legal/compliance approval, production approval, canlı sinyal, broker talimatı, model deployment veya yatırım tavsiyesi değildir.

## Local Final Closing
- Final master terminal lock rehearsal nasıl okunur?
- Ultimate offline project constitution neden official constitution değildir?
- Final non-production seal rehearsal neden official seal değildir?
- Local-only terminal archive index neden gerçek archive/ZIP değildir?
- Closing governance super-binder nasıl yorumlanır?
- Final closeout readiness score neden approval değildir?
- Phase 100 final katmanının production, live trading, deployment veya yatırım tavsiyesi olmadığı açık yazılsın.
- Gerçek lock, seal, handover, release, build, deployment, canlı emir, broker execution ve yatırım tavsiyesi olmadığı açık yazılsın.



## Phase 105/106 Safety
- Phase 105 raporlarının yatırım tavsiyesi olmadığı garanti edilmiştir.
- Phase 106 handoff'un scraping izni olmadığı denetlenir.
- Provider requirement'ın credential istemediği kontrol edilir.
- Broker/live/deployment sınırlarının devam ettiği temin edilmiştir.



## Phase 106 Safety Guidelines
- Scraping yapılmadığı
- HTML scraping/browser automation/paywall bypass/hidden API reverse engineering/rate abuse yasak olduğu
- Provider placeholder'ın gerçek API çağrısı olmadığı
- Provider request'in yatırım tavsiyesi veya broker talimatı olmadığı
- Credential yazdırılmayacağı
- Canlı trading/deployment sınırlarının devam ettiği



## Phase 107 FX Provider Safety
- FX katmanında scraping yapılmadığı kesindir.
- HTML scraping/browser automation/paywall bypass/hidden API reverse engineering/rate abuse yasaktır.
- FX provider placeholder'ın gerçek API çağrısı olmadığı ve sinyal üretmediği kesindir.
- FX provider request'in yatırım tavsiyesi veya broker talimatı olmadığı bilinmelidir.
- FX pair registry'nin sinyal olmadığı belirtilir.
- Credential yazdırılmayacağı kurallara bağlıdır.
- Canlı trading/deployment sınırlarının devam ettiği unutulmamalıdır.


### Phase 108 Safe Usage
- Commodity katmanında scraping yapılmadığı garanti edilir.
- HTML scraping/browser automation/paywall bypass/hidden API reverse engineering/rate abuse yasaktır.
- Commodity provider placeholder’ın gerçek API çağrısı olmadığı teyit edilmiştir.
- Futures metadata placeholder’ın futures işlem sistemi olmadığı belirtilmiştir.
- Commodity provider request’in yatırım tavsiyesi veya broker/futures broker talimatı olmadığı sabittir.
- Commodity universe registry’nin sinyal olmadığı sabittir.
- Credential yazdırılmayacağı kurallara bağlıdır.
- Canlı trading/deployment sınırlarının devam ettiği onaylanmıştır.

Phase 109 Macro katmanında scraping yapılmadığı, credential yazdırılmayacağı
## Phase 110 Safety
- Economic Calendar provider katmanı hiçbir scraping veya canlı trading/broker işlemine izin vermez.
- Local-only, research-only, non-production zorunludur.

## Phase 111 News Metadata Provider Safety
- Haber katmanında web scraping, HTML scraping, haber sayfası scraping, browser automation (Puppeteer/Playwright/Selenium), paywall bypass ve gizli API tersine mühendisliği kesinlikle yasaktır.
- Telifli içerik ve haber tam metni kopyalanmaz, depolanmaz veya çekilmez; yalnızca metadata/ref/etiket sözleşmesi kullanılır.
- Sentiment ve impact alanları kesin sinyal, yön tahmini veya alım-satım talimatı değildir; downstream fazlar için kategorik sözleşmedir.
- Canlı trading, broker emir iletimi, gerçek emir, yatırım tavsiyesi, directional kesinlik iddiası yasaktır.
- Credential ve API key hiçbir log veya ekranda yazdırılmaz, koda veya repoya gömülmez.
- Model deployment, NLP model servisi, harici LLM API çağrısı, vector DB ve embedding kullanımı yapılmaz.
- Dry-run default True, local-only True, non-production True ve research-only True olarak işletilir.

## Phase 112 Data Quality Engine Safe Usage Guidelines
- **Yıkıcı Eylem Yasağı**: Kalite anomali tespiti sonrasında hiçbir dosya silinmez, üzerine yazılmaz (auto-overwrite) veya satır bazında elenmez. `destructive_action_allowed` sabiti daima `False` kalır; tüm anomaliler `manual_review_queue` ve Phase 113 handoff'a yönlendirilir.
- **Skor Güvenliği**: Provider Quality Score ve Dataset Quality Score yalnızca dahili bir teşhis ve araştırma metriğidir. Asla trade sinyali, portföy ağırlığı veya resmi sağlayıcı akreditasyonu/onayı olarak kullanılamaz.
- **Telif ve Anti-Scraping Sınırı**: Kalite motoru, `news_copyright_quality_rules` ile haber verilerinde tam metin (`full_text`, `article_body`, `scraped_html`) ve harici LLM/vektör veritabanı referanslarını denetler; tespit ettiğinde `quality_critical` seviyesinde durdurma üretir.
- **Credential Sızıntı Koruması**: Sağlayıcı metadata tablolarında `api_key`, `secret`, `token` veya şifre bulunması `quality_critical` anomali olarak yakalanır; credential sızıntısı engellenir.
- **Çalışma Modu**: `advanced_data_quality_enabled=True`, `dry_run=True`, `local_only=True`, `allow_live_trading=False`. Mevcut faz 112, bir sonraki faz 113, nihai hedef 160.

## Phase 113 Data Normalization Layer Safe Usage Guidelines
- **Canlı İşlem ve Sinyal Yasağı**: Data Normalization Layer canlı trading yapmaz, broker API'sine bağlanmaz, gerçek emir iletmez ve yatırım tavsiyesi üretmez. Normalize edilmiş veriler veya şemalar asla alım-satım veya yön sinyali olarak sunulamaz.
- **Resmi Onay İddiası Yasağı**: Normalization skoru veya uygunluk doğrulaması resmi onay, lisanslama, uyum sertifikası veya broker hazırlık iddiası değildir; yalnızca dahili araştırma kalitesi metriğidir.
- **Non-Destructive Normalizasyon Garantisi**: Kaynak veriler (`raw_df`) üzerinde in-place değişiklik yapılmaz. Hiçbir kaynak dosya silinmez, taşınmaz veya üzerine yazılmaz (`source_preserved: True`, `destructive_action_allowed: False`).
- **Yıkıcı Temizleme Yasağı**: Outlier silme, filtreleme veya duplicate kayıtları düşürme (deduplication) kesinlikle yapılmaz. Mükerrer kayıtlar için yalnızca `canonical_duplicate_key` üretilir ve veri bütünlüğü korunur.
- **Scraping ve Telif Hakkı Güvenliği**: Web scraping, HTML parsing, haber sayfası scraping, tarayıcı otomasyonu (Selenium/Playwright), paywall bypass veya gizli API tersine mühendisliği kesinlikle yasaktır. Haber tam metni veya telifli içerik kopyalanamaz ve depolanamaz; yalnızca üst veri (metadata) düzeyinde çalışılır.
- **Credential Güvenliği**: Çıktılarda, tablolarda, raporlarda veya loglarda API anahtarı, gizli anahtar (secret) veya token yazdırılamaz.
- **Manuel İnceleme Güvenliği**: `manual_review_normalization_queue` yalnızca insan denetimi, Phase 114 soy kütüğü kaydı ve Phase 115 benchmark'ı için referans sağlar; otomatik silme/düzeltme önerisi yapmaz.
- **Çalışma Rejimi**: `data_normalization_dry_run_default=True`, `data_normalization_local_only=True`, `data_normalization_research_only=True`, `data_normalization_allow_live_trading=False`. Mevcut faz: 113, Bir sonraki faz: 114, Nihai hedef: 160.

## Phase 114 Data Lineage and Provenance Safe Usage Guidelines
- **Canlı İşlem ve Sinyal Yasağı**: Data Lineage and Provenance katmanı canlı trading yapmaz, broker API'sine bağlanmaz, gerçek portföy işlemi veya alım/satım tavsiyesi üretmez. Soy kütüğü bağlantıları veya izlenebilirlik skorları asla al/sat sinyali veya alfa faktörü olarak yorumlanamaz.
- **Resmi Onay İddiası Yasağı**: Traceability score veya provider provenance kayıtları hiçbir şekilde "resmi onay", "akreditasyon" veya "production deployment onayı" niteliğinde değildir; yalnızca sistem içi tanı metriğidir.
- **Kaynak Veri Dokunulmazlığı**: Hiçbir kaynak veri dosyası üzerine yazılmaz (no overwrite), tahrif edilmez veya silinmez. Tüm soy kütüğü kayıtlarında `source_preserved: True` ve `destructive_action_allowed: False` koşulları zorunludur.
- **Sıfır Scraping ve Sıfır Tam Metin**: Web scraping, haber sayfası tarama, HTML kazıma, paywall aşma veya tam makale metni indirme kesinlikle yasaktır (`metadata_only: True`). Telifli haber ve makale metinleri kopyalanamaz veya depolanamaz.
- **Kimlik Bilgisi ve Gizlilik Güvenliği**: Kaynak referanslarında veya sağlayıcı metadata kayıtlarında API anahtarları, parolalar veya gizli kimlik dizgileri bulunamaz (`contains_credentials: False`).
- **Harici Graph ve Vector DB Yasağı**: Lineage graph placeholder tamamen bellek içi (in-memory) pandas tablolarından ibarettir. Harici graph database (Neo4j vb.), vector database veya embedding API çağrıları kesinlikle kullanılmaz.
- **Çalışma Rejimi**: `data_lineage_dry_run_default=True`, `data_lineage_local_only=True`, `data_lineage_research_only=True`, `data_lineage_allow_live_trading=False`. Mevcut faz: 114, Bir sonraki faz: 115 (Data Provider Benchmark Report), Nihai hedef: 160.

## Phase 115 Data Provider Benchmark Report Safe Usage Guidelines
- **Canlı İşlem ve Sinyal Yasağı**: Data Provider Benchmark Report katmanı canlı trading yapmaz, broker API'sine bağlanmaz, gerçek emir iletmez ve finansal yatırım tavsiyesi üretmez. Benchmark skoru kesinlikle al/sat sinyali veya trade tetiği olarak kullanılamaz.
- **Resmi Onay ve Hazırlık İddiası Yasağı**: Benchmark skoru veya araştırma sıralaması sağlayıcıyı resmi onaylı (`official_approval: False`), canlı kullanıma hazır (`production_ready: False`) veya broker uyumlu (`broker_ready: False`) ilan etmez.
- **Gerçek API Çağrısı ve Veri İndirme Zorunluluğu Yoktur**: Benchmark katmanı tamamen yerel ve offline çalışır; canlı sağlayıcı API çağrısı veya gerçek veri indirme zorunluluğu içermez.
- **Sıfır Scraping ve Sıfır Tam Metin**: Web scraping, HTML kazıma, tarayıcı otomasyonu (Selenium/Playwright), paywall aşma veya telifli haber tam metni toplama kesinlikle yasaktır (`metadata_only: True`).
- **Kaynak Veri Dokunulmazlığı ve Yıkıcı Temizlik Yasağı**: Kaynak veriler üzerinde overwrite, silme veya tahrifat yapılmaz. Manuel inceleme kuyruklarında `destructive_action_allowed: False` garantisi korunur.
- **Harici LLM, Web Sunucusu ve Vektör DB Yasağı**: Flask/FastAPI, dashboard, bulut yapay zeka modelleri veya vektör veri tabanları sisteme dahil edilmez.
- **Phase 116 Feature Engine Handoff**: Rapor yalnızca gösterge/faktör motoru (Phase 116) için veri hazırlık gereksinimlerini aktarır; gösterge, faktör veya sinyal üretmez.
- **Çalışma Rejimi**: `advanced_provider_benchmark_enabled=True`, `provider_benchmark_dry_run_default=True`, `provider_benchmark_local_only=True`, `provider_benchmark_research_only=True`, `provider_benchmark_allow_live_trading=False`. Mevcut faz: 115, Bir sonraki faz: 116 (Advanced Indicator/Feature/Factor Engine), Nihai hedef: 160.

## Phase 116 Advanced Indicator/Feature/Factor Engine Safe Usage Guidelines
- **Kesin Non-Signal Prensibi**: Feature veya teknik gösterge değerleri hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi veya trade kararı olarak kullanılamaz (`non_signal: True`). "RSI < 30 -> AL" veya "SMA kesişimi -> LONG" gibi yönlü kurallar kesinlikle yasaktır.
- **Yasaklı Kolon Adı Koruması**: Çıktı DataFrame'lerinde ve feature store tablolarında `signal`, `buy`, `sell`, `long`, `short`, `position`, `target`, `label`, `prediction`, `recommendation` kolon adları yasaklanmıştır ve validasyon kuralı ile denetlenir.
- **Non-Destructive Hesaplama Güvencesi**: Tüm feature hesaplama fonksiyonları `df.copy()` ile çalışır. Girdi veri çerçevesi in-place değiştirilemez; ham veri kaynakları ve lake dosyaları ezilemez (`no_source_overwrite: True`).
- **Lookahead Bias ve Veri Sızıntısı Koruması**: Negatif shift (`shift(-1)`) ve ileriye dönük indeksleme kesinlikle yasaktır. Rolling pencereleri geçmiş ve mevcut barları kullanır (`[t - window + 1 : t]`); ilk `window - 1` satırdaki NaN değerler maskelenir veya korunur, sentetik veri uydurulmaz.
- **Sıfır Web Scraping ve Sıfır Haber Tam Metni**: Web scraping, haber sayfası kazıma, HTML indirme, paywall baypas etme veya telifli haber metnini kopyalama kesinlikle yasaktır (`metadata_only: True`). Yalnızca konu etiketi, varlık etiketi ve zaman tazeliği metadata olarak işlenir.
- **Canlı İşlem, Broker ve Model Dağıtım Yasağı**: Broker API entegrasyonu, gerçek emir iletimi, backtest çalıştırma, parametre optimizasyonu, ML model eğitimi/dağıtımı, web sunucusu veya UI dashboard başlatma kesinlikle yasaktır.
- **Çalışma Rejimi**: `advanced_feature_engine_enabled=True`, `feature_engine_dry_run_default=True`, `feature_engine_local_only=True`, `feature_engine_research_only=True`, `feature_engine_allow_live_trading=False`. Mevcut faz: 116, Bir sonraki faz: 117 (Technical Indicator Expansion), Nihai hedef: 160.

## Phase 117 Technical Indicator Expansion Safe Usage Guidelines
- **Kesin Non-Signal ve Sıfır Canlı Emir**: Phase 117 Technical Indicator Expansion katmanı hiçbir koşulda al/sat sinyali, trade tavsiyesi, kesin yön kararı, long/short emri veya portföy talimatı üretmez (`non_signal: True`, `allow_indicator_as_signal: False`).
- **Yasaklı Kolon Adı Koruması**: Çıktı kolonlarında `signal`, `buy`, `sell`, `long`, `short`, `position`, `target`, `label`, `prediction`, `recommendation`, `future_return`, `forward_return`, `next_return` kelimeleri yer alamaz. MACD sinyal çizgisi `macd_smooth` olarak zorunlu tutulmuştur.
- **TA-Lib Bağımsızlığı**: Bütün göstergeler TA-Lib veya harici C kütüphanesi gerektirmeden saf Python/pandas/numpy ile hesaplanır; sistem bağımlılık riski taşımaz.
- **Lookahead Bias ve Veri Bütünlüğü**: `shift(-1)` veya geleceğe dönük zaman kaydırmaları kesinlikle yasaktır; tüm hesaplamalar `df.copy()` ile başlar ve orijinal girdi verisini mutasyona uğratmaz.
- **Isınma NaN Dokunulmazlığı**: Başlangıç pencere eksik verileri `preserve_nan_no_fill_no_delete` politikası ile korunur; sentetik veya uydurma veri eklenmez.
- **Strateji, Backtest ve Optimizer Yasağı**: İndikatörler üzerinde kural tabanlı strateji üretilemez, backtest koşturulamaz ve parametre optimizasyonu yapılamaz (`allow_strategy_generation: False`, `allow_backtest_execution: False`, `allow_optimizer_execution: False`).
- **Sıfır Web Scraping ve Sıfır Gizli Anahtar Sızıntısı**: Canlı web kazıma, tarayıcı otomasyonu veya gizli API anahtarı loglama kesinlikle yasaktır.
- **Çalışma Rejimi**: `technical_indicators_enabled=True`, `technical_indicators_dry_run_default=True`, `technical_indicators_local_only=True`, `technical_indicators_research_only=True`, `technical_indicators_allow_live_trading=False`. Mevcut faz: 117, Bir sonraki faz: 118 (Multi-Window Feature Grid), Nihai hedef: 160.

## Phase 118 Multi-Window Feature Grid Safe Usage Guidelines
- **Kesin Non-Signal ve Sıfır Canlı Emir İlkesi**: Phase 118 Multi-Window Feature Grid katmanı hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, hedef getiri tahmini, long/short pozisyonu veya broker talimatı üretmez (`non_signal: True`, `allow_feature_grid_as_signal: False`).
- **Yasaklı Kolon Adı ve İddia Koruması**: Üretilen grid tablolarında `signal`, `buy`, `sell`, `long`, `short`, `position`, `target`, `label`, `prediction`, `recommendation`, `future_return`, `forward_return` kolon adları kesinlikle yasaktır ve validasyon kuralları tarafından denetlenir.
- **Sıfır In-Place Mutasyon (Non-Destructive Transform)**: Tüm grid hesaplamaları `out = df.copy()` ile çalışır. Girdi DataFrame'i asla yerinde değiştirilemez; orijinal seriler mutlak dokunulmazlığa sahiptir.
- **No-Lookahead Bias ve Negatif Shift Yasağı**: Kod içerisinde `shift(-1)` veya geleceğe dönük zaman serisi projeksiyonları kesinlikle yasaktır. Tüm pencereler geçmiş ve anlık bar verileriyle sınırlıdır (`strictly backward-looking`).
- **Warmup NaN Koruma Politikası**: Büyük pencerelerin (ör. `w200`) ilk satırlarındaki NaN değerler sentetik doldurmaya tabi tutulmaz (`no_forward_fill`) veya silinmez (`no_auto_drop`). Doğal veri seyri korunur (`preserve_warmup_nan`).
- **Mükerrer Feature Yönetimi**: Benzer veya aynı seriyi üreten feature kolonları otomatik silinmez; analist değerlendirmesi için bayraklandırılır (`flag_for_manual_review`).
- **Strateji, Backtest, Optimizer ve Model Eğitimi Yasağı**: Feature grid çıktıları üzerinde kural tabanlı strateji üretilemez, backtest koşturulamaz ve optimizer çalıştırılamaz (`allow_strategy_generation: False`, `allow_backtest_execution: False`, `allow_optimizer_execution: False`).
- **Sıfır Web Scraping ve Sıfır Gizli Anahtar Sızıntısı**: Canlı web kazıma, tarayıcı otomasyonu, haber tam metni indirme veya API anahtarlarının loglara dökülmesi kesinlikle yasaktır.
- **Çalışma Rejimi**: `advanced_feature_grid_enabled=True`, `feature_grid_dry_run_default=True`, `feature_grid_local_only=True`, `feature_grid_research_only=True`, `feature_grid_allow_live_trading=False`, `feature_grid_allow_feature_grid_as_signal=False`. Mevcut faz: 118, Bir sonraki faz: 119 (Cross-Asset Feature Alignment), Nihai hedef: 160.

## Phase 119 Cross-Asset Feature Alignment Safe Usage Guidelines
- **Kesin Non-Signal ve Arbitraj/Pairs Sinyali Yasağı**: Phase 119 Cross-Asset Feature Alignment katmanı hiçbir koşulda AL/SAT sinyali, varlıklar arası arbitraj sinyali, pairs trading kuralı, spread al-sat tavsiyesi veya broker talimatı üretmez (`non_signal: True`, `allow_arbitrage_signals: False`).
- **Kesin Geriye Dönük (Backward-Only) Asof Birleştirme Zorunluluğu**: Zaman serisi birleştirmelerinde yalnızca `direction="backward"` kullanılabilir (`safe_asof_join_backward`). Gelecekte gerçekleşen bir makro açıklamanın veya haber olayının geçmiş fiyata eşlenmesi (forward nearest join) kesinlikle yasaktır.
- **Target, Label ve Prediction Üretim Yasağı**: Matrislerde ve manifestlerde `target`, `label`, `prediction`, `future_return`, `forward_return` gibi yönlü/tahminsel kolonlar kesinlikle yer alamaz (`contains_target_or_prediction: False`).
- **Sıfır In-Place Mutasyon (Non-Destructive Transform)**: Tüm birleştirme ve matris oluşturma operasyonları `df.copy()` ile çalışır. Kaynak veri dosyalarının veya girdi DataFramelerinin üzerine yazılması (source overwrite) kesinlikle yasaktır.
- **Standart Ad Alanı ve Yasaklı Terim Koruması**: `<domain>__<family>__<source_symbol>__<feature_name>__<window>` ad alanı standardı uygulanır; `signal`, `buy`, `sell`, `long`, `short`, `position`, `target`, `label`, `prediction` gibi yönlü kelimeler isimlerde yer alamaz.
- **Sıfır Web Scraping ve Sıfır Haber Tam Metni**: Web scraping, HTML kazıma, haber sayfası tarama veya telifli haber tam metninin indirilmesi kesinlikle yasaktır; yalnızca metadata (konu, varlık etiketi, duygu skoru) kullanılır.
- **Çalışma Rejimi**: `cross_asset_alignment_enabled=True`, `cross_asset_alignment_dry_run_default=True`, `cross_asset_alignment_local_only=True`, `cross_asset_alignment_research_only=True`, `cross_asset_alignment_allow_live_trading=False`. Mevcut faz: 119, Bir sonraki faz: 120 (Macro/Calendar/News Feature Fusion & Composite Factor Engineering), Nihai hedef: 160.

## Phase 120 Macro/Calendar/News Feature Fusion Safe Usage Guidelines
- **Kesin Non-Signal ve Olay Tabanlı Al/Sat Yasağı**: Phase 120 Macro/Calendar/News Feature Fusion katmanı hiçbir koşulda AL/SAT sinyali, makro trade tetiği, takvim duyuru tahmini, olay bazlı işlem kuralı veya broker talimatı üretmez (`non_signal: True`, `allow_fusion_as_signal: False`).
- **Makro Yayın Gecikmesi ve No-Lookahead Güvencesi**: Makro veriler geriye dönük olarak fiyata bağlanırken kesinlikle `release_timestamp <= base_timestamp` koşulu aranır (`macro_release_lag_policies.py`). Açıklanmamış makro verilerin analize dahil edilmesi kesinlikle engellenir.
- **Sıfır İleriye Dönük (No-Forward) Asof Birleştirme**: Birleştirme operasyonlarında yalnızca `direction="backward"` kullanılabilir (`safe_fusion_asof_join_backward`). İleriye dönük eşleştirmeler veri sızıntısı sayılır.
- **Sıfır Web Scraping ve Sıfır Haber Tam Metni**: Web scraping, HTML kazıma, haber sayfası tarama veya telifli haber metninin indirilmesi/kopyalanması kesinlikle yasaktır (`metadata_only: True`). Yalnızca konu etiketi, varlık etiketi, zaman damgası tazeliği ve olay referansı kullanılır.
- **Target, Label ve Prediction Üretim Yasağı**: Fusion matrislerinde `target`, `label`, `prediction`, `future_return`, `forward_return` gibi modelleme kolonları kesinlikle yer alamaz.
- **Sıfır In-Place Mutasyon (Non-Destructive Transform)**: Bütün birleştirme ve dönüşüm fonksiyonları `df.copy()` ile çalışır. Kaynak serilerin üzerine yazılması yasaktır.
- **28 No-Go ve 14 Safe-Go Kuralı Uygulaması**: Sistem 28 No-Go sınırını ve 14 Safe-Go ilkesini her çalıştırmada doğrular (`fusion_feature_safety_boundary.py`).
- **Çalışma Rejimi**: `advanced_feature_fusion_enabled=True`, `feature_fusion_dry_run_default=True`, `feature_fusion_local_only=True`, `feature_fusion_research_only=True`, `feature_fusion_allow_live_trading=False`. Mevcut faz: 120, Bir sonraki faz: 121 (Feature Validation and No-Lookahead Guard), Nihai hedef: 160.

## Phase 121 Feature Validation and No-Lookahead Guard Safe Usage Guidelines
- **Kesin Non-Signal Prensibi**: Feature doğrulama katmanı kesinlikle AL/SAT tavsiyesi veya trade kararı üretmez (`non_signal: True`, `allow_validation_as_signal: False`). Doğrulama skoru al-sat güveni veya model başarısı göstergesi olarak kullanılamaz.
- **Sıfır Lookahead / Sıfır Sızıntı**: Negatif shift (`shift(-1)`), geleceğe dönük getiri (`future_return`) veya geleceğe dönük zaman birleştirmesi tespit edildiğinde downstream işlemler bloklanır.
- **Yasaklı Kolon Denetimi ve Karantina**: `buy`, `sell`, `signal`, `target`, `label`, `prediction`, `position`, `recommendation` içeren kolonlar tespit edildiğinde karantinaya alınır.
- **Tahribatsız İnceleme Güvencesi (`destructive_action_allowed: False`)**: NaN değerler, duplicate kolonlar veya sayısal outlier'lar sessizce silinmez, üzerine yazılmaz; bulgu kaydı oluşturulup manuel inceleme kuyruğuna iletilir.
- **Resmi Onay ve Canlıya Hazırlık İddiası Yasağı**: Raporlar ve çıktılar hiçbir sistem veya feature için resmi onay (`official_approval: False`), canlıya hazır (`production_ready: False`) veya broker uyumlu (`broker_ready: False`) iddiasında bulunamaz.
- **Çalışma Rejimi**: `feature_validation_enabled=True`, `feature_validation_dry_run_default=True`, `feature_validation_local_only=True`, `feature_validation_research_only=True`, `feature_validation_allow_live_trading=False`. Mevcut faz: 121, Bir sonraki faz: 122 (Factor Metadata and Factor Families), Nihai hedef: 160.

## Phase 122 Factor Metadata and Factor Families Safe Usage Guidelines
- **Kesin Non-Signal ve Alfa/Sinyal Üretim Yasağı**: Faktör metadata ve taksonomi katmanı hiçbir koşulda AL/SAT tavsiyesi, emir, broker talimatı, alfa sinyali, model tahmini veya hedef getiri üretmez (`non_signal: True`, `allow_factor_as_signal: False`).
- **Yasaklı İddia ve Terim Denetimi**: Faktör tanımlarında, sözleşmelerinde veya manifestlerinde 14 yasaklı regex deseni (`buy signal`, `sell signal`, `trade recommendation`, `alpha forecast`, `long entry`, `short exit` vb.) kesinlikle yasaktır.
- **Tahribatsız Yönetişim İlkesi (`destructive_action_allowed: False`, `source_preserved: True`)**: Kusurlu veya eksik tanımlanmış faktör girdileri asla sessizce silinmez, kaynak dosyaların üzerine yazılmaz. Bütün tespitler Manuel İnceleme Kuyruğuna (`manual_review_queue`) yönlendirilir.
- **Strictly Metadata-Only Haber Dikkat Faktörleri**: Haber faktörleri yalnızca frekans, varlık etiketleri ve tazelik derecesi gibi sayısal metadata ile sınırlıdır. Web scraping, haber sayfası tarama, telifli tam metin kopyalama, NLP sentiment modeli veya vektör embedding kullanımı kesinlikle yasaktır.
- **20 NO-GO ve 11 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 20 NO-GO şartını doğrular (canlı trading, broker API, lookahead sızıntısı, dosya silme vb.) ve 11 SAFE-GO ilkesini aktif tutar (`factor_metadata_safety_boundary.py`).
- **Resmi Onay ve Canlıya Hazırlık İddiası Yasağı**: Faktör sözleşmeleri ve manifest kayıtları hiçbir koşulda resmi onay (`official_approval: False`), canlıya hazır (`production_ready: False`) veya broker hazır (`broker_ready: False`) iddiasında bulunamaz.
- **Çalışma Rejimi**: `factor_metadata_enabled=True`, `factor_metadata_dry_run_default=True`, `factor_metadata_local_only=True`, `factor_metadata_research_only=True`, `factor_metadata_allow_live_trading=False`. Mevcut faz: 122, Bir sonraki faz: 123 (Feature Quality and Drift Diagnostics), Nihai hedef: 160.

## Phase 123 Feature Quality and Drift Diagnostics Safe Usage Guidelines
- **Kesin Non-Signal ve Alfa/Sinyal Üretim Yasağı**: Feature kalite ve drift tanı katmanı hiçbir koşulda AL/SAT tavsiyesi, emir, broker talimatı, alfa sinyali, model tahmini veya hedef getiri üretmez (`non_signal: True`, `allow_quality_drift_as_signal: False`). Drift skorları ve kararlılık metrikleri asla alım-satım tetikleyicisi olarak yorumlanamaz.
- **Yasaklı İddia ve Terim Denetimi**: Kalite ve drift raporlarında, kuyruklarında veya manifest kayıtlarında `buy_signal`, `sell_signal`, `target_column`, `future_return`, `article_body_raw`, `web_scraping_enabled`, `auto_drop_executed`, `auto_impute_executed`, `live_trading_approved`, `official_production_approval` gibi terimler kesinlikle yasaktır (`validate_no_forbidden_quality_drift_claims`).
- **Tahribatsız Yönetişim İlkesi (`destructive_action_allowed: False`, `auto_drop_allowed: False`, `auto_fix_allowed: False`, `source_preserved: True`)**: Kusurlu, aşırı eksik (all-NaN, %100 missing), sonsuz (inf) veya yüksek kayma gösteren kolonlar asla otomatik silinmez (auto-drop), doldurulmaz (auto-impute) veya kaynak dosyalar yerinde ezilmez. Bütün kusurlar Manuel İnceleme Kuyruklarına (`quality_manual_review_queue`, `drift_manual_review_queue`) yönlendirilir.
- **Strictly Metadata-Only Makro, Takvim ve Haber Sınırı**: Makro, takvim ve haber metaveri kalite kontrollerinde yalnızca sayısal metadata, zaman damgaları ve kategorik etiketler doğrulanır. Web kazıma (scraping), tarayıcı otomasyonu, telifli tam metin kopyalama, NLP duygu modelleri veya vektör embedding kullanımı kesinlikle yasaktır.
- **13 NO-GO ve 7 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 13 NO-GO şartını denetler (canlı trading, broker API, drift'i sinyal olarak kullanma, otomatik silme/doldurma vb.) ve 7 SAFE-GO ilkesini aktif tutar (`feature_quality_drift_safety_boundary.py`).
- **Resmi Onay ve Canlıya Hazırlık İddiası Yasağı**: Kalite ve drift skorları veya manifest kayıtları hiçbir koşulda resmi onay (`official_approval: False`), canlıya hazır (`production_ready: False`) veya broker hazır (`broker_ready: False`) iddiasında bulunamaz.
- **Çalışma Rejimi**: `advanced_feature_quality_drift_enabled=True`, `feature_quality_drift_dry_run_default=True`, `feature_quality_drift_local_only=True`, `feature_quality_drift_research_only=True`, `feature_quality_drift_allow_live_trading=False`. Mevcut faz: 123, Bir sonraki faz: 124 (Feature Store Integration and Time-Series Feature Serving Layer), Nihai hedef: 160.
## Phase 124 Feature Store Integration Safe Usage Guidelines
- **Kesin Non-Signal ve Depo-Sinyal Eşleştirme Yasağı**: Feature Store entegrasyon katmanı yalnızca çevrimdışı/yerel araştırma metaverisi depolar; hiçbir şekilde alım-satım sinyali, emir, broker talimatı, alfa tetiği veya model tahmini olarak kullanılamaz (`non_signal: True`, `allow_feature_store_as_signal: False`).
- **Yasaklı İddia ve Kolon Denetimi**: Depo tablolarında, şemalarında, manifestolarında veya raporlarında `buy`, `sell`, `signal`, `target`, `label`, `prediction`, `recommendation`, `future_return`, `full_text`, `article_body`, `scraped_html`, `embedding`, `vector`, `official approval`, `production ready`, `broker ready` terimleri kesinlikle yasaktır (`validate_no_forbidden_feature_store_claims`).
- **Tahribatsız Yönetişim ve Kaynak Koruma İlkesi (`destructive_action_allowed: False`, `auto_drop_allowed: False`, `auto_fix_allowed: False`, `source_preserved: True`)**: Kusurlu veya inceleme bekleyen kolonlar asla otomatik silinmez veya ezilmez. Bütün tespitler Manuel İnceleme Engelleyicilerine (`manual_review_blockers`) kaydedilir.
- **Strictly Metadata-Only Haber Depolama Sınırı**: Haber depolarında yalnızca sayısal metadata, zaman damgaları ve kategorik etiketler bulunur. Web kazıma (scraping), telifli tam metin kopyalama, duygu modelleri veya vektör embedding kullanımı kesinlikle yasaktır.
- **15 NO-GO ve 8 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 15 NO-GO şartını denetler (canlı ticaret, broker API, store'u sinyal olarak kullanma, dosya ezme vb.) ve 8 SAFE-GO ilkesini aktif tutar (`feature_store_integration_safety_boundary.py`).
- **Resmi Onay ve Canlıya Hazırlık İddiası Yasağı**: Depo sözleşmeleri ve manifestolar hiçbir koşulda resmi onay (`official_approval: False`), üretime hazır (`production_ready: False`) veya aracı kurum entegrasyonu hazır (`broker_ready: False`) iddiasında bulunamaz.
- **Çalışma Rejimi**: `feature_store_integration_enabled=True`, `feature_store_integration_dry_run_default=True`, `feature_store_integration_local_only=True`, `feature_store_integration_research_only=True`, `feature_store_integration_allow_live_trading=False`. Mevcut faz: 124, Bir sonraki faz: 125 (Feature/Factor Engine Acceptance Report and Block Finalization), Nihai hedef: 160.

## Phase 125 Feature/Factor Engine Acceptance Report Safe Usage Guidelines
- **Kesin Non-Signal ve Kabul Skoru Sınırı**: Phase 125 Feature/Factor Engine Acceptance Report katmanı yalnızca çevrimdışı ve yapısal blok kabulü gerçekleştirir; hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya model tahmini üretmez (`non_signal: True`, `allow_acceptance_as_signal: False`).
- **Kabul Skoru Asla Finansal Onay Değildir**: Üretilen kabul skoru (0.0 - 1.0 aralığı), 16 kabul geçidinin sözleşmesel ve teknik bütünlük metriğidir. Asla kârlılık garantisi, resmi uygunluk veya finansal getiri güvencesi olarak yorumlanamaz (`official_approval: False`, `production_ready: False`, `broker_ready: False`).
- **Yasaklı İddia ve Kolon Denetimi**: Kabul raporlarında, manifestolarda, tablolarda veya kuyruklarda `buy`, `sell`, `signal`, `target`, `label`, `prediction`, `recommendation`, `future_return`, `full_text`, `article_body`, `scraped_html`, `official approval`, `production ready`, `broker ready` terimleri kesinlikle yasaktır (`validate_no_forbidden_feature_factor_acceptance_claims`).
- **Tahribatsız Yönetişim ve Kaynak Koruma İlkesi (`destructive_action_allowed: False`, `auto_drop_allowed: False`, `auto_fix_allowed: False`, `source_preserved: True`)**: Blok kabulü sürecinde hiçbir ham veri, özellik veya faktör tablosu silinmez, üzerine yazılmaz veya sentetik doldurulmaz. Tüm açık noktalar Manuel İnceleme Kuyruğuna (`manual_review_queue`) yönlendirilir.
- **Strictly Metadata-Only Haber Denetimi**: Haber girdileri yalnızca zaman damgası, konu ve varlık etiketleri gibi metaverilerle sınırlıdır. Web scraping, telifli tam metin kopyalama veya harici LLM duygu modeli kullanımı kesinlikle yasaktır.
- **15 NO-GO ve 7 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 15 NO-GO kuralını denetler (canlı ticaret, broker API, kabulü sinyal sayma, veri silme vb.) ve 7 SAFE-GO ilkesini aktif tutar (`feature_engine_block_safety_boundary.py`).
- **Phase 126 Regime Handoff Güvencesi**: Blok kabulü tamamlandıktan sonra, rejim modelleri için 12 yapılandırılmış girdi öğesi Phase 126 (Regime Classification and Market Behavior Foundation) fazına devredilir; rejim katmanının da non-signal kalacağı baştan taahhüt edilir.
- **Çalışma Rejimi**: `advanced_feature_factor_acceptance_enabled=True`, `feature_factor_acceptance_dry_run_default=True`, `feature_factor_acceptance_local_only=True`, `feature_factor_acceptance_research_only=True`, `feature_factor_acceptance_allow_live_trading=False`. Mevcut faz: 125, Bir sonraki faz: 126 (Regime Classification and Market Behavior Foundation), Nihai hedef: 160.

## Phase 126 Regime Classification and Market Behavior Foundation Safe Usage Guidelines
- **Kesin Non-Signal ve Rejim Sinyal Eşleştirme Yasağı**: Phase 126 Regime Classification and Market Behavior Foundation katmanı yalnızca çevrimdışı ve tanımlayıcı piyasa durumu taksonomisi oluşturur; hiçbir koşulda AL/SAT sinyali, long/short pozisyon talimatı, emir, broker talimatı, alfa tetiği veya model tahmini üretmez (`non_signal: True`, `allow_regime_as_signal: False`).
- **Rejim Durumları Asla Ticaret Önerisi Değildir**: Çıktılanan rejim durumları (trend, range, yüksek/düşük volatilite vb.), piyasanın fiziksel ve istatistiksel çevresel bağlamıdır. "Trend rejimi var → hemen long aç" veya "volatilite yüksek → hemen short aç" gibi yorumlar sistem seviyesinde kesinlikle yasaklanmıştır (`allow_directional_claim: False`).
- **Model Eğitimi, Kümeleme ve Tahmin Yasağı**: Bu fazda hiçbir denetimli/denetimsiz makine öğrenimi eğitimi (HMM, GMM, k-means, clustering, SVM) veya model tahmini yürütülmez (`allow_model_training: False`, `allow_clustering_execution: False`, `allow_target_label_generation: False`, `allow_prediction_generation: False`). Yalnızca taksonomik ve sözleşmesel temel oluşturulur.
- **Yasaklı İddia ve Kolon Denetimi**: Rejim şemalarında, manifestolarda, tablolarda veya kuyruklarda `buy`, `sell`, `signal`, `target`, `label`, `prediction`, `recommendation`, `future_return`, `full_text`, `article_body`, `scraped_html`, `official approval`, `production ready`, `broker ready` terimleri kesinlikle yasaktır (`validate_no_forbidden_regime_claims`).
- **Tahribatsız Yönetişim ve Kaynak Koruma İlkesi (`destructive_action_allowed: False`, `auto_drop_allowed: False`, `auto_fix_allowed: False`, `source_preserved: True`)**: Rejim taksonomisi sürecinde hiçbir ham veri, indikatör veya faktör tablosu silinmez, üzerine yazılmaz veya sentetik doldurulmaz. Tüm açık noktalar ve anomaliler Manuel İnceleme kuyruklarına yönlendirilir.
- **Strictly Metadata-Only Haber Denetimi**: Haber bağlam girdileri yalnızca frekans, varlık etiketleri ve tazelik derecesi gibi sayısal metadata ile sınırlıdır. Web scraping, telifli tam metin kopyalama veya harici LLM duygu modeli kullanımı kesinlikle yasaktır.
- **15 NO-GO ve 8 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 15 NO-GO kuralını denetler (canlı ticaret, broker API, rejimi sinyal sayma, veri ezme vb.) ve 8 SAFE-GO ilkesini aktif tutar (`regime_foundation_safety_boundary.py`).
- **Phase 127 Regime Feature Matrix Handoff Güvencesi**: Temel taksonomi ve sözleşmeler tamamlandıktan sonra, rejim matrisi için 12 yapılandırılmış devir maddesi Phase 127 (Regime Feature Matrix and State Dataset Contracts) fazına devredilir; rejim feature matrisinin de non-signal ve model-eğitimsiz kalacağı baştan taahhüt edilir.
- **Çalışma Rejimi**: `advanced_regime_foundation_enabled=True`, `regime_foundation_dry_run_default=True`, `regime_foundation_local_only=True`, `regime_foundation_research_only=True`, `regime_foundation_allow_live_trading=False`. Mevcut faz: 126, Bir sonraki faz: 127 (Regime Feature Matrix and State Dataset Contracts), Nihai hedef: 160.

## Phase 127 Regime Feature Matrix and State Dataset Contracts Safe Usage Guidelines
- **Kesin Non-Signal ve Veri Kümesi Güvenliği**: Phase 127 Regime Feature Matrix and State Dataset Contracts katmanı yalnızca araştırma amaçlı feature matrisleri ve durum veri kümesi sözleşmeleri üretir; hiçbir koşulda AL/SAT sinyali, pozisyon büyüklüğü, hedef getiri, model tahmini veya yatırım tavsiyesi içermez (`non_signal: True`, `contains_target_or_prediction: False`).
- **Aday Bağlamlar Asla Hedef Değişken Değildir**: Tanımlanan 10 aday bağlam bayrağı (`candidate_contexts`) yalnızca araştırma amaçlı filtre ve koşul etiketleridir; hiçbir zaman gözetimli modelleme hedefi (`target_label`), supervised sınıf etiketi veya prediction olarak kullanılamaz (`candidate_contexts_as_targets: False`).
- **Model Eğitimi ve Kümeleme Kesinlikle Yasaktır**: Bu fazda HMM, GMM, K-Means veya herhangi bir makine öğrenimi modeli eğitilmez veya çalıştırılmaz (`allow_model_training: False`, `allow_clustering_execution: False`). Yalnızca veri kümesi sözleşmeleri ve şemalar dondurulur.
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Asof join birleştirmelerinde yalnızca geriye-dönük (`direction='backward'`) yöntem kullanılır. `context_timestamp <= base_timestamp` temporal sıralaması kesinlikle korunmalıdır. `shift(-1)`, negatif shift, `lead()` ve `t+1` kullanımı guard motoru tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Temizlik**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Ham dosyalar ezilmez (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik sütun düşürme (`auto_feature_drop: False`) kesinlikle yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **Strictly Metadata-Only Haber Kuralı**: Haber girdileri yalnızca ilgi skoru, zaman damgası ve varlık etiketleri gibi metaverileri içerir. Web kazıma (scraping), telifli tam metin kopyalama veya NLP duygu modelleri kullanımı yasaktır.
- **15 NO-GO ve 7 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 15 NO-GO şartını denetler (canlı ticaret, broker API, matrisi sinyal sayma, veri ezme, negatif shift vb.) ve 7 SAFE-GO ilkesini aktif tutar (`regime_matrix_safety_boundary.py`).
- **Phase 128 Unsupervised Regime Discovery Handoff Güvencesi**: Matris ve durum sözleşmeleri tamamlandıktan sonra, 12 yapılandırılmış devir maddesi Phase 128 (Regime Rule-Free Labeling Contracts and Unsupervised Prep) fazına devredilir.
- **Çalışma Rejimi**: `advanced_regime_matrix_enabled=True`, `regime_matrix_dry_run_default=True`, `regime_matrix_local_only=True`, `regime_matrix_research_only=True`, `regime_matrix_allow_live_trading=False`. Mevcut faz: 127, Bir sonraki faz: 128 (Regime Rule-Free Labeling Contracts and Unsupervised Prep), Nihai hedef: 160.

## Phase 128 Regime Rule-Free Labeling Contracts and Unsupervised Prep Safe Usage Guidelines
- **Kesin Non-Signal ve Aday Durum Güvenliği**: Phase 128 Regime Rule-Free Labeling Contracts and Unsupervised Prep katmanı yalnızca araştırma amaçlı aday durum sözleşmeleri, pseudo-durum şemaları ve denetimsiz hazırlık kuralları tanımlar; hiçbir koşulda AL/SAT sinyali, pozisyon büyüklüğü, hedef getiri, model tahmini veya alım-satım tavsiyesi içermez (`non_signal: True`, `contains_target_or_prediction: False`).
- **"Rule-Free Labeling" Asla Ticaret Etiketi veya Supervised Target Değildir**: Rule-free labeling, salt denetimsiz candidate state hazırlık sözleşmesidir. Asla gelecekteki getiriye dayalı hedef değişken veya yönlü sınıf etiketi üretilmez.
- **Model Eğitimi, Kümeleme ve Boyut İndirgeme Yürütme Kesinlikle Yasaktır**: Bu fazda KMeans, DBSCAN, GMM, HDBSCAN, SOM kümeleme algoritmaları, PCA, UMAP, t-SNE boyut indirgeme yöntemleri veya herhangi bir makine öğrenimi modeli eğitilmez, fit/predict veya transform yürütülmez (`clustering_executed: False`, `model_training_executed: False`, `dimensionality_reduction_executed: False`). Yalnızca sözleşmeler ve yer tutucu defterleri oluşturulur.
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: `context_ts <= base_ts` kuralı zorunludur. Negatif kaydırma (`shift(-1)`), `lead()`, `t+1` ve geleceğe referans veren kolonlar no-lookahead guard motoru tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Koruma ve Sıfır Yıkıcı Temizlik**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Ham dosyalar ezilmez (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_feature_drop: False`) kesinlikle yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **Strictly Metadata-Only Haber Kuralı**: Haber girdileri yalnızca zaman damgası, frekans ve varlık etiketlerini içerir. Web kazıma (scraping), telifli tam metin saklama veya NLP modelleri kullanımı yasaktır.
- **16 NO-GO ve 8 SAFE-GO Güvenlik Sınırı**: Sistem her çalıştırmada 16 NO-GO kuralını denetler (canlı ticaret, broker API, kural-bağımsız adayları sinyal sayma, kümeleme çalıştırma, model eğitimi, veri ezme vb.) ve 8 SAFE-GO ilkesini aktif tutar (`regime_rule_free_safety_boundary.py`).
- **Phase 129 Handoff Güvencesi**: 12 yapılandırılmış devir maddesi ile Phase 129 (Market Behavior Diagnostics and Regime Quality) fazına devir tamamlanmıştır.
- **Çalışma Rejimi**: `advanced_regime_rule_free_enabled=True`, `regime_rule_free_dry_run_default=True`, `regime_rule_free_local_only=True`, `regime_rule_free_research_only=True`, `regime_rule_free_allow_live_trading=False`. Mevcut faz: 128, Bir sonraki faz: 129 (Market Behavior Diagnostics and Regime Quality), Nihai hedef: 160.

## Phase 129 Market Behavior Diagnostics and Regime Quality Safe Usage Guidelines
- **Kesin Non-Signal ve Davranış Kalitesi Güvenliği**: Phase 129 Market Behavior Diagnostics and Regime Quality katmanı yalnızca araştırma amaçlı piyasa davranış tanıları, aday durum kalite ölçümleri ve rejim ailesi kapsama raporları üretir. Hiçbir koşulda AL/SAT sinyali, long/short yönü, strateji tavsiyesi, pozisyon büyüklüğü, hedef getiri veya model tahmini içermez (`non_signal: True`, `allow_quality_as_signal: False`, `allow_behavior_as_signal: False`).
- **Behavior Quality Score Kesinlikle Sinyal veya Tahmin Değildir**: Kalite skoru ([0.0, 1.0]) saf bir iç teşhis metriğidir. "Kalite yüksek → alım zamanı", "trend kalitesi iyi → long pozisyon" veya benzeri yorumlar yapılması kesinlikle yasaktır (`allow_directional_claim: False`, `official_approval: False`).
- **Model Eğitimi, Kümeleme, Backtest ve Optimizer Yürütme Kesinlikle Yasaktır**: Bu fazda KMeans, DBSCAN, GMM, HDBSCAN, SOM gibi kümeleme algoritmaları; PCA, UMAP, t-SNE gibi boyut indirgeme yöntemleri; herhangi bir ML model fit/train/predict; backtest motoru; portföy optimizer kesinlikle çalıştırılamaz (`clustering_executed: False`, `model_training_executed: False`, `unsupervised_execution: False`, `backtest_executed: False`). Yalnızca tanı ve kalite raporları üretilir.
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Davranış tanı raporlarında `shift(-1)`, `lead()`, `t+1`, `future_return` ve geleceğe referans veren kolonlar kesinlikle kullanılamaz.
- **Kaynak Koruma ve Sıfır Yıkıcı İşlem**: Davranış tanı raporları kaynak verileri değiştirmez, üzerine yazmaz veya sıfırlamaz. `auto_imputation: False`, `auto_drop: False`, `source_overwrite: False`, `destructive_action_allowed: False`. Tüm girdi çerçeveleri `df.copy()` ile korunur.
- **Haber Metadata-Only Sınırı**: Haber metadata behavior diagnostics'te kesinlikle haber tam metni, `article_body`, web scraping, NLP duygu modeli, embedding veya GPT/LLM metin özeti kullanılamaz (`contains_full_text: False`, `allow_web_scraping: False`, `sentiment_model_executed: False`).
- **16 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 16 NO-GO kuralını (canlı ticaret, broker API bağlantısı, davranış tanıyı sinyal sayma, model eğitimi, veri ezme, scraping vb.) ve 8 SAFE-GO ilkesini denetler (`market_behavior_diagnostics_safety_boundary.py`). Tüm güvenlik kontrolleri SECURE sonuç vermeli; herhangi bir BREACH durumunda sistem otomatik hata verir.
- **Phase 130 Handoff Güvencesi**: 12 yapılandırılmış devir maddesi ile Phase 130 (Regime Transition and Stability Analysis) fazına devir eksiksiz tamamlanmıştır.
- **Çalışma Rejimi**: `advanced_market_behavior_diagnostics_enabled=True`, `behavior_diagnostics_dry_run_default=True`, `behavior_diagnostics_local_only=True`, `behavior_diagnostics_research_only=True`, `behavior_diagnostics_allow_live_trading=False`. Mevcut faz: 129, Bir sonraki faz: 130 (Regime Transition and Stability Analysis), Nihai hedef: 160.

## Phase 130 Regime Transition and Stability Analysis Safe Usage Guidelines
- **Kesin Non-Signal ve Rejim Geçiş Güvenliği**: Phase 130 Regime Transition and Stability Analysis katmanı yalnızca araştırma amaçlı rejim geçiş sıklığı, geçiş matris yer tutucuları, aday durum sürekliliği ve geçiş kararlılık tanıları üretir. Hiçbir koşulda AL/SAT sinyali, long/short pozisyon talimatı, emir, broker talimatı, alfa tetiği veya model tahmini üretmez (`non_signal: True`, `allow_transition_as_signal: False`, `allow_stability_as_signal: False`).
- **Geçiş ve Kararlılık Metrikleri Asla Ticaret Kuralı Değildir**: Aday durum geçiş hazırlığı (`transition_readiness`), kalıcılık skoru (`persistence_score`) veya kararlılık indeksi (`stability_score`), piyasa rejiminin içsel homojenliğini ve ardışıklık yapısını ölçen istatistiksel tanımlayıcılardır. "Geçiş hazırlığı yüksek → alım yap" ya da "kararlılık düştü → pozisyon kapat" gibi çıkarımlar yasaktır (`allow_directional_claim: False`).
- **Model Eğitimi, Kümeleme, Markov Fit ve Tahmin Yasağı**: Bu fazda Markov zinciri olasılık uydurma (`markov_chain_fitted: False`), ileriye dönük durum olasılık projeksiyonu (`forward_probability_generated: False`), kümeleme (KMeans, DBSCAN, GMM vb.) veya makine öğrenimi modeli eğitimi/çıkarımı (`model_training_allowed: False`, `clustering_allowed: False`) kesinlikle yürütülmez. Matrisler yalnızca yapısal yer tutucu niteliğindedir.
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Asof join ve geçiş sırası analizlerinde strictly backward-looking (`direction='backward'`) sıralama esastır. Gözlem zamanı `t`, bağlam zamanı `t_ctx <= t` kuralına kesinlikle uymalıdır. `shift(-1)`, negatif kaydırma, `lead()`, `t+1` ve geleceğe referans veren etiketler no-lookahead guard tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak dosyalar üzerine yazılmaz (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_feature_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`, `auto_fix_allowed: False`).
- **Strictly Metadata-Only Haber Kuralı**: Haber bağlam girdileri yalnızca başlık akışı konu etiketleri, tazelik gecikmesi ve frekans bilgisiyle sınırlıdır. Web scraping, telifli tam metin kopyalama veya NLP duygu modelleri kullanımı yasaktır (`full_text_used: False`, `scraping_executed: False`, `embedding_used: False`, `sentiment_model_used: False`).
- **18 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 18 NO-GO kuralını (canlı ticaret, broker API, geçişi sinyal sayma, Markov fit, veri ezme, scraping vb.) ve 8 SAFE-GO ilkesini denetler (`regime_transition_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 131 Cross-Asset Context Handoff Güvencesi**: 9 yapılandırılmış devir maddesi ile Phase 131 (Cross-Asset Regime Context and Dynamic Behavior Interplay) fazına devir eksiksiz hazırlanmıştır.
- **Çalışma Rejimi**: `advanced_regime_transition_enabled=True`, `regime_transition_dry_run_default=True`, `regime_transition_local_only=True`, `regime_transition_research_only=True`, `regime_transition_allow_live_trading=False`. Mevcut faz: 130, Bir sonraki faz: 131 (Cross-Asset Regime Context and Dynamic Behavior Interplay), Nihai hedef: 160.

## Phase 131 Cross-Asset Regime Context Expansion Safe Usage Guidelines
- **Kesin Non-Signal ve Çapraz Varlık Bağlam Güvenliği**: Phase 131 Cross-Asset Regime Context Expansion katmanı yalnızca araştırma amaçlı çoklu-etki alanı rejim bağlamı, birlikte hareket (co-movement), ayrışma/yakınsama (divergence/convergence), volatilite/trend/range bağlantıları ve makro duyarlılık tanıları üretir. Hiçbir koşulda AL/SAT sinyali, ikili işlem (pairs trading) stratejisi, arbitraj tetiği, pozisyon büyüklüğü, getiri beklentisi veya model tahmini içermez (`non_signal: True`, `allow_context_as_signal: False`, `allow_pairs_trading_signals: False`).
- **Korelasyon, Ayrışma ve Bağlantı Metrikleri Asla Ticaret Sinyali Değildir**: Co-movement skoru, ayrışma indeksi veya rejim geçiş uyumu metrikleri, FX ve emtia piyasalarının makro ve rejim düzeyindeki yapısal ilişkilerini inceleyen tanısal araçlardır. "Ayrışma yükseldi → pairs trade aç", "korelasyon kırıldı → arbitraj yap" gibi çıkarımlar kesinlikle yasaktır (`allow_directional_claim: False`).
- **Model Eğitimi, Kümeleme ve Tahmin Yürütme Kesinlikle Yasaktır**: Bu fazda hiçbir makine öğrenmesi modeli (denetimli veya denetimsiz), kümeleme algoritması (KMeans, DBSCAN, GMM vb.), regresyon modeli veya optimizatör çalıştırılamaz; model fit/train/predict operasyonları yasaktır (`clustering_executed: False`, `model_training_executed: False`).
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Asof join birleştirmelerinde yalnızca backward yönü (`direction='backward'`) zorunludur. `context_timestamp <= base_timestamp` temporal sırası korunmalı; `shift(-1)`, negatif shift, `lead()`, `future_return` ve geleceğe referans veren etiketler no-lookahead guard tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak tablolar ezilemez (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_feature_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **Strictly Metadata-Only Haber Kuralı**: Haber etki alanı bağlamında kesinlikle haber tam metni, `article_body`, web kazıma (scraping), NLP duygu modeli veya embedding kullanılamaz (`full_text_used: False`, `scraping_executed: False`, `sentiment_model_used: False`).
- **18 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 18 NO-GO kuralını (canlı ticaret, broker API, bağlamı sinyal sayma, pairs trading, model eğitimi, veri ezme, scraping vb.) ve 8 SAFE-GO ilkesini denetler (`cross_asset_regime_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 132 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 132 (Macro, Event & News Regime Context Expansion) fazına devir eksiksiz hazırlanmıştır (`phase_132_handoff.py`).
- **Çalışma Rejimi**: `advanced_cross_asset_regime_context_enabled=True`, `cross_asset_regime_dry_run_default=True`, `cross_asset_regime_local_only=True`, `cross_asset_regime_research_only=True`, `cross_asset_regime_allow_live_trading=False`. Mevcut faz: 131, Bir sonraki faz: 132 (Macro, Event & News Regime Context Expansion), Nihai hedef: 160.

## Phase 132 Macro/Event/News Regime Context Expansion Safe Usage Guidelines
- **Kesin Non-Signal ve Çevresel Bağlam Güvenliği**: Phase 132 Macro/Event/News Regime Context Expansion katmanı yalnızca araştırma amaçlı makroekonomik göstergeler, ekonomik takvim olayları ve haber metaveri rejim bağlamları üretir. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği, model tahmini veya getiri beklentisi içermez (`non_signal: True`, `allow_context_as_signal: False`).
- **Makro, Takvim ve Haber Değerleri Asla Ticaret Kuralı Değildir**: Enflasyon, istihdam, faiz oranları, takvim olay pencereleri ve haber dikkat yoğunlukları piyasanın rejim arka planını tarifler. "CPI yüksek geldi → hemen short aç", "FOMC penceresindeyiz → hemen volatilite pozisyonu al" gibi çıkarımlar sistem seviyesinde kesinlikle yasaklanmıştır (`allow_directional_claim: False`).
- **Strictly Metadata-Only Haber Sınırı**: Haber tarafında yalnızca sayısal metadata, konu etiketleri, varlık etiketleri, makro tematik etiketler, olay bağlantıları, zaman damgaları ve kaynak ID'leri kullanılır. Haber tam metni, makale gövdesi (`article_body`), ham içerik (`raw_content`), web kazıma (`scraped_html`), NLP duygu modelleri (`sentiment_score`) veya embedding/vektör veri tabanı kullanımı kesinlikle yasaktır ve boundary guard ile engellenir.
- **Model Eğitimi, Kümeleme, Backtest ve Optimizasyon Yasağı**: Bu fazda hiçbir makine öğrenmesi modeli (HMM, GMM, KMeans, DBSCAN vb.) eğitilemez veya çalıştırılamaz; model fit/train/predict operasyonları, backtest ve portföy optimizasyonu kesinlikle yasaktır (`model_training_executed: False`, `clustering_executed: False`, `unsupervised_execution: False`).
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Asof join birleştirmelerinde yalnızca backward yönü (`direction='backward'`) zorunludur. Verinin planlanan yayın tarihi ile fiili gözlemlenme zamanı karşılaştırılır; verinin erken sızdırılması (`actual < scheduled`) veya geleceğe kaydırma (`shift(-1)`, `lead()`, `future_return`) no-lookahead guard tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak tablolar ezilemez (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **20 NO-GO ve 9 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 20 NO-GO kuralını (canlı ticaret, broker API, bağlamı sinyal sayma, model eğitimi, haber metni çekme, scraping, veri ezme vb.) ve 9 SAFE-GO ilkesini denetler (`macro_event_news_regime_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 133 Handoff Güvencesi**: 13 yapılandırılmış devir maddesi ile Phase 133 (Regime Validation and No-Lookahead Acceptance) fazına devir eksiksiz hazırlanmıştır (`phase_133_handoff.py`).
- **Çalışma Rejimi**: `advanced_macro_event_news_regime_enabled=True`, `macro_event_news_regime_dry_run_default=True`, `macro_event_news_regime_local_only=True`, `macro_event_news_regime_research_only=True`, `macro_event_news_regime_allow_live_trading=False`. Mevcut faz: 132, Bir sonraki faz: 133 (Regime Validation and No-Lookahead Acceptance), Nihai hedef: 160.

## Phase 133 Regime Validation and No-Lookahead Acceptance Safe Usage Guidelines
- **Kesin Non-Signal ve Kabul Skoru Güvenliği**: Phase 133 Regime Validation and No-Lookahead Acceptance katmanı yalnızca çevrimdışı ve yerel araştırma rejim doğrulaması ve no-lookahead kabulü gerçekleştirir. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği, model tahmini veya getiri beklentisi içermez (`non_signal: True`, `allow_acceptance_as_signal: False`).
- **Kabul Skoru Asla Finansal Onay Değildir**: 1.0 bileşik kabul skoru (`high_acceptance_integrity`), 19 kabul geçidinin mimari, sözleşmesel ve teknik bütünlük durumunu ifade eder; asla kârlılık, getiri güvencesi veya canlıya geçiş onayı olarak yorumlanamaz (`official_approval: False`, `production_ready: False`, `broker_ready: False`).
- **Model Eğitimi, Kümeleme, Backtest ve Optimizasyon Yasağı**: Bu fazda hiçbir makine öğrenmesi modeli (HMM, GMM, KMeans, DBSCAN vb.) eğitilemez veya çalıştırılamaz; model fit/train/predict operasyonları, kümeleme algoritması yürütmesi, backtest ve portföy optimizasyonu kesinlikle yasaktır (`allow_model_training: False`, `allow_clustering_execution: False`, `allow_target_label_generation: False`, `allow_prediction_generation: False`).
- **Strictly Metadata-Only Haber Sınırı**: Haber tarafında yalnızca sayısal metadata, konu etiketleri, varlık etiketleri, makro tematik etiketler, olay bağlantıları, zaman damgaları ve kaynak ID'leri kabul edilir. Haber tam metni, makale gövdesi (`article_body`), ham içerik (`raw_content`), web kazıma (`scraped_html`), NLP duygu modelleri (`sentiment_score`) veya embedding/vektör veri tabanı kullanımı kesinlikle yasaktır ve boundary guard ile engellenir.
- **Zaman Damgası Hizalama ve Sıfır Lookahead Bias**: Asof join birleştirmelerinde yalnızca backward yönü (`direction='backward'`) zorunludur. `context_ts <= base_ts` kuralı korunur; verinin erken sızdırılması veya geleceğe kaydırma (`shift(-1)`, `lead()`, `future_return`, `forward_return`) no-lookahead guard tarafından engellenir (`allow_lookahead: False`).
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak tablolar silinemez veya ezilemez (`allow_source_overwrite: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_feature_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **22 NO-GO ve 10 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 22 NO-GO kuralını (canlı ticaret, broker API, kabulü sinyal sayma, model eğitimi, kümeleme çalıştırma, haber metni çekme, scraping, veri ezme vb.) ve 10 SAFE-GO ilkesini denetler (`regime_validation_acceptance_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 134 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 134 (Regime FeatureStore Integration) fazına devir eksiksiz hazırlanmıştır (`phase_134_handoff.py`).
- **Çalışma Rejimi**: `advanced_regime_validation_acceptance_enabled=True`, `regime_validation_acceptance_dry_run_default=True`, `regime_validation_acceptance_local_only=True`, `regime_validation_acceptance_research_only=True`, `regime_validation_acceptance_allow_live_trading=False`. Mevcut faz: 133, Bir sonraki faz: 134 (Regime FeatureStore Integration), Nihai hedef: 160.

## Phase 134 Regime FeatureStore Integration Safe Usage Guidelines
- **Kesin Non-Signal ve FeatureStore Güvenliği**: Phase 134 Regime FeatureStore Integration katmanı yalnızca çevrimdışı ve yerel araştırma rejim metaverisi depolama, kataloglama ve sözleşme disiplini sağlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği, model tahmini veya getiri beklentisi içermez (`non_signal: True`, `allow_featurestore_as_signal: False`).
- **FeatureStore Hazır Bulunuşluk Skoru Asla Finansal Onay Değildir**: 1.0 hazır bulunuşluk skoru (`readiness_score: 1.0`), FeatureStore sözleşmelerinin ve katalog metaverilerinin teknik uyum durumunu ifade eder; asla kârlılık, getiri güvencesi veya canlıya geçiş onayı olarak yorumlanamaz (`official_approval: False`, `production_ready: False`, `broker_ready: False`).
- **Model Eğitimi, Kümeleme, Backtest ve Optimizasyon Yasağı**: Bu fazda hiçbir makine öğrenmesi modeli (HMM, GMM, KMeans, DBSCAN vb.) eğitilemez veya çalıştırılamaz; model fit/train/predict operasyonları, kümeleme algoritması yürütmesi, denetimsiz segmentasyon, backtest ve portföy optimizasyonu kesinlikle yasaktır (`model_training_executed: False`, `model_fit_executed: False`, `model_predict_executed: False`, `clustering_executed: False`, `unsupervised_execution: False`).
- **Strictly Metadata-Only Haber Sınırı**: Haber tarafında yalnızca sayısal metadata, konu etiketleri, varlık etiketleri, makro tematik etiketler, olay bağlantıları, zaman damgaları ve kaynak ID'leri kabul edilir. Haber tam metni, makale gövdesi (`article_body`), ham içerik (`raw_content`), web kazıma (`scraped_html`), NLP duygu modelleri (`sentiment_score`) veya embedding/vektör veri tabanı kullanımı kesinlikle yasaktır ve boundary guard ile engellenir.
- **Yerel Okuma ve Tahribatsız Yazma Disiplini**: Okuma sözleşmeleri kesinlikle yerel dosya sistemiyle sınırlıdır; ağ çağrısı, broker entegrasyonu, canlı emir veya credential kullanımı yasaktır. Yazma sözleşmeleri yalnızca metaveri ekleme (`append`) ve zaman damgalı anlık görüntü (`snapshot`) ile sınırlıdır; kaynak dosyaların üzerine yazılması (`allow_source_overwrite: False`), silinmesi (`allow_file_deletion: False`), tahribatlı temizlik (`allow_destructive_clean: False`), otomatik doldurma (`allow_auto_imputation: False`) ve otomatik özellik silme (`allow_auto_feature_drop: False`) kesinlikle yasaktır (`source_preserved: True`).
- **Sorgu Kısıtları ve Yasaklı Kolon Koruması**: FeatureStore sorguları yalnızca izin verilen 8 metaveri filtresiyle yapılabilir. 12 yasaklı sorgu terimi (`buy`, `sell`, `signal`, `target`, `prediction`, `recommendation` vb.) ve 23 yasaklı kolon adı (`future_return`, `full_text`, `article_body`, `raw_content`, `scraped_html`, `embedding`, `vector`, `sentiment_score` vb.) kesinlikle engellenir.
- **21 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 21 NO-GO kuralını (canlı ticaret, broker API, depoyu sinyal sayma, model eğitimi, kümeleme çalıştırma, haber metni çekme, scraping, veri ezme vb.) ve 8 SAFE-GO ilkesini denetler (`regime_featurestore_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 135 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 135 (Regime Classification Acceptance Report) fazına devir eksiksiz hazırlanmıştır (`phase_135_handoff.py`).
- **Çalışma Rejimi**: `advanced_regime_featurestore_integration_enabled=True`, `regime_featurestore_dry_run_default=True`, `regime_featurestore_local_only=True`, `regime_featurestore_research_only=True`, `regime_featurestore_allow_live_trading=False`. Mevcut faz: 134, Bir sonraki faz: 135 (Regime Classification Acceptance Report), Nihai hedef: 160.

## Phase 135 Regime Classification Acceptance Report and Block Finalization Safe Usage Guidelines
- **Phase 126-135 Blok Kapanışı ve Kesin Non-Signal Güvencesi**: Phase 135 Regime Classification Acceptance Report katmanı, Phase 126-134 arasındaki 10 fazlık blokta geliştirilen tüm rejim ve piyasa davranışı bileşenlerini uçtan uca kabul raporu ve imzalı manifestoya bağlar. Raporlar, kabul skorları, geçit durumları ve manifestolar asla AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya model tahmini içermez (`non_signal: True`, `allow_acceptance_as_signal: False`).
- **1.0 Kabul Skoru ve ACCEPTANCE_PASS Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0 bileşik kabul skoru ve 17 kabul geçidinin PASS olması, yalnızca araştırma kodunun, sözleşmelerin ve mimarinin teknik/yönetişimsel bütünlüğünü belgeler. Asla kârlılık, getiri güvencesi, sermaye riski onayı veya broker API yetkilendirmesi olarak yorumlanamaz (`official_approval: False`, `production_ready: False`, `broker_ready: False`).
- **Model Eğitimi, Kümeleme, Fitting ve Denetimsiz Öğrenme Kesinlikle Yasaktır**: Bu fazda hiçbir makine öğrenmesi modeli (denetimli veya denetimsiz), kümeleme algoritması (KMeans, DBSCAN, GMM, HMM vb.) eğitilemez veya çalıştırılamaz; model fit/train/predict operasyonları, kümeleme yürütmesi, hedef etiket türetimi ve tahmin üretimi kesinlikle yasaktır (`model_training_executed: False`, `model_fit_executed: False`, `model_predict_executed: False`, `clustering_executed: False`, `unsupervised_execution: False`).
- **Strictly Metadata-Only Haber Sınırı**: Haber etki alanı bağlamında yalnızca sayısal metaveri, konu/varlık etiketleri, zaman damgaları ve kaynak ID'leri kullanılır. Haber tam metni, makale gövdesi (`article_body`), ham içerik (`raw_content`), web kazıma (`scraped_html`), NLP duygu modelleri (`sentiment_score`) veya embedding/vektör veri tabanı kullanımı kesinlikle yasaktır ve boundary guard ile taranır.
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak veri tabloları ezilemez (`allow_source_overwrite: False`), silinemez (`allow_file_deletion: False`), otomatik veri doldurma (`auto_imputation: False`) ve otomatik özellik düşürme (`auto_feature_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **19 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 19 NO-GO kuralını (canlı ticaret, broker API, kabulü sinyal sayma, model eğitimi, kümeleme çalıştırma, haber metni çekme, scraping, veri ezme vb.) ve 8 SAFE-GO ilkesini denetler (`regime_block_safety_boundary.py`). Tüm denetimler SECURE olmalıdır.
- **Phase 136 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 136 (GPU Acceleration and Advanced ML Runtime Foundation) fazına devir eksiksiz hazırlanmıştır (`phase_136_handoff.py`).
- **Çalışma Rejimi**: `advanced_regime_acceptance_enabled=True`, `regime_acceptance_dry_run_default=True`, `regime_acceptance_local_only=True`, `regime_acceptance_research_only=True`, `regime_acceptance_allow_live_trading=False`. Mevcut faz: 135, Bir sonraki faz: 136 (GPU Acceleration and Advanced ML Runtime Foundation), Nihai hedef: 160.

## Phase 136 GPU Acceleration and Advanced ML Runtime Foundation Safe Usage Guidelines
- **Kesin Non-Signal ve Runtime Foundation Güvenliği**: Phase 136 GPU Acceleration and Advanced ML Runtime Foundation katmanı yalnızca çevrimdışı ve yerel araştırma donanım/runtime keşfi, hızlandırıcı yetenek envanteri ve güvenli ML deney sözleşmeleri sağlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği, model tahmini veya getiri beklentisi içermez (`non_signal: True`, `allow_runtime_as_signal: False`).
- **Model Eğitimi, İnferans/Tahmin ve Hedef Etiket Üretim Yasağı**: Bu fazda hiçbir makine öğrenmesi modeli eğitilemez (`fit`, `train`), hiçbir inferans veya tahmin üretilemez (`predict`, `transform`), hiçbir hedef etiket veya gelecek getiri kolonu türetilemez (`future_return`, `forward_return`, `next_return`, `shift(-1)`). İzin verilen operasyonlar yalnızca donanım keşfi, ortam anlık görüntüsü, backend tespiti ve deney izin politikalarıdır (`allowed_now` vs `blocked_now`).
- **Kümeleme, Ansambl, Kalibrasyon ve Duygu Modeli Yasağı**: Hiçbir kümeleme algoritması (KMeans, DBSCAN, GMM vb.), ansambl modeli yürütmesi, olasılık kalibrasyonu, NLP duygu modeli veya harici embedding/vektör veritabanı operasyonu çalıştırılamaz (`clustering_executed: False`, `ensemble_executed: False`, `calibration_executed: False`).
- **Strictly Metadata-Only Haber Sınırı**: Haber etki alanı bağlamında yalnızca sayısal metaveri, konu etiketleri, varlık etiketleri, makro tematik etiketler, zaman damgaları ve kaynak ID'leri kullanılır. Haber tam metni, makale gövdesi (`article_body`), ham içerik (`raw_content`), web kazıma (`scraped_html`), NLP duygu modelleri (`sentiment_score`) veya embedding/vektör veri tabanı kullanımı kesinlikle yasaktır ve boundary guard ile denetlenir.
- **Kaynak Veri Dokunulmazlığı ve Sıfır Yıkıcı Müdahale**: Girdi veri çerçeveleri asla yerinde değiştirilmez (`df.copy()` zorunludur). Kaynak veri tabloları ezilemez (`allow_source_overwrite: False`), silinemez (`allow_file_deletion: False`), tahribatlı temizlik (`allow_destructive_clean: False`), otomatik veri doldurma (`allow_auto_imputation: False`) ve otomatik özellik düşürme (`allow_auto_feature_drop: False`) yasaktır (`destructive_action_allowed: False`, `source_preserved: True`).
- **25 NO-GO ve 10 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 25 NO-GO kuralını (canlı ticaret, broker API, donanımı sinyal sayma, model eğitimi, tahmin üretimi, hedef etiket türetme, kümeleme çalıştırma, haber metni çekme, scraping, veri ezme vb.) ve 10 SAFE-GO ilkesini denetler (`gpu_ml_runtime_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazır Bulunuşluk Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0 hazır bulunuşluk skoru (`readiness_score: 1.0`), donanım keşfinin, hızlandırıcı profillerinin ve ML güvenlik sözleşmelerinin teknik uyumunu belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir (`official_approval: False`, `production_ready: False`, `broker_ready: False`).
- **Phase 137 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 137 (Advanced Feature Transformation, Normalization & Scaling for ML) fazına devir eksiksiz hazırlanmıştır (`phase_137_handoff.py`).
- **Çalışma Rejimi**: `advanced_gpu_ml_runtime_enabled=True`, `gpu_ml_runtime_dry_run_default=True`, `gpu_ml_runtime_local_only=True`, `gpu_ml_runtime_research_only=True`, `gpu_ml_runtime_allow_live_trading=False`. Mevcut faz: 136, Bir sonraki faz: 137 (Advanced Feature Transformation, Normalization & Scaling for ML), Nihai hedef: 160.

## Phase 145 Advanced ML Acceptance Report and Consolidated Acceptance Layer Safe Usage Guidelines
- **Kesin Non-Signal ve Kabul Sözleşmesi Güvenliği**: Phase 145 Advanced ML Acceptance Report katmanı yalnızca çevrimdışı ve yerel araştırma blok kabul sözleşmeleri, konsolide kabul denetimleri, güvenlik sınırları ve Phase 146 devir hazırlığı sağlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Gerçek Model Eğitimi, Tahmin ve Çıkarım (Zero Training/Inference)**: Model eğitimi, parametre uydurma, tahmin veya çıkarım kesinlikle yapılmaz (`training_allowed: False`, `inference_allowed: False`).
- **Sıfır Model Kayıt Defteri Yazımı ve Nesne Kalıcılığı (Zero Registry Write)**: MLflow, S3 veya disk üzerine model ağırlıkları veya binary serileştirmeleri kaydedilmez (`model_registry_written: False`, `artifact_persisted: False`).
- **Sıfır Canlı/Production Onayı (Zero Production Approval)**: Kabul raporları, bileşen kontrol noktaları ve hazırlık skorları asla üretim onayı, dağıtım yetkisi veya broker hazır durumu onayı değildir (`production_approved: False`, `broker_ready_approved: False`, `official_approval: False`).
- **34 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 34 NO-GO kuralını (canlı ticaret, broker API, kabul raporunu sinyal sayma, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, registry yazma, haber metni çekme vb.) ve 8 SAFE-GO ilkesini denetler (`advanced_ml_acceptance_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`advanced_ml_contract_acceptance_ready_non_production`), Advanced ML bloğunun sözleşmesel ve yönetişimsel eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 146 Handoff Güvencesi**: 13 yapılandırılmış devir maddesi ile Phase 146 (Realistic Backtest, Transaction Cost and Slippage Modeling) fazına devir eksiksiz hazırlanmıştır (`phase_146_handoff.py`).
- **Çalışma Rejimi**: `advanced_ml_acceptance_enabled=True`, `advanced_ml_acceptance_dry_run_default=True`, `advanced_ml_acceptance_local_only=True`, `advanced_ml_acceptance_research_only=True`, `advanced_ml_acceptance_allow_live_trading=False`. Mevcut faz: 145, Bir sonraki faz: 146 (Realistic Backtest, Transaction Cost and Slippage Modeling), Nihai hedef: 160.

## Phase 146 Realistic Backtest, Transaction Cost and Slippage Modeling Safe Usage Guidelines
- **Kesin Non-Signal ve Sözleşme Güvenliği**: Phase 146 Realistic Backtest katmanı yalnızca çevrimdışı ve yerel araştırma backtest motor sözleşmeleri, işlem maliyet ve kayma modelleri ile muhasebe yaşam döngüsü sözleşmelerini tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı piyasa bağlantısı, gerçek broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Backtest Yürütmesi ve Optimizasyon**: Canlı veya gerçek backtest motoru çalıştırılamaz, parametre optimizasyonu yapılamaz, walk-forward simülasyonu çalıştırılamaz, benchmark veya Monte Carlo stres testi koşturulamaz (`allow_backtest_execution: False`, `allow_optimizer_execution: False`, `allow_walk_forward_execution: False`, `allow_benchmark_execution: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **35 NO-GO ve 10 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 35 NO-GO kuralını (canlı ticaret, broker API, backtest sözleşmesini sinyal sayma, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 10 SAFE-GO ilkesini denetler (`safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`READY_FOR_PHASE_147_WALK_FORWARD_HANDOFF`), backtest ve maliyet modelleme katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 147 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 147 (Walk-Forward Validation and Out-of-Sample Testing) fazına devir eksiksiz hazırlanmıştır (`phase_147_handoff.py`).
- **Çalışma Rejimi**: `advanced_realistic_backtest_enabled=True`, `realistic_backtest_dry_run_default=True`, `realistic_backtest_local_only=True`, `realistic_backtest_research_only=True`, `realistic_backtest_allow_live_trading=False`. Mevcut faz: 146, Bir sonraki faz: 147 (Walk-Forward Validation and Out-of-Sample Testing), Nihai hedef: 160.

## Phase 147 Walk-Forward Validation and Out-of-Sample Benchmarking Safe Usage Guidelines
- **Kesin Non-Signal ve Doğrulama Sözleşmesi Güvenliği**: Phase 147 Walk-Forward Validation katmanı yalnızca çevrimdışı ve yerel araştırma split sözleşmelerini, OOS benchmark şablonlarını, bias/sızıntı korumalarını ve doğrulama manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Doğrulama Yürütmesi ve Optimizasyon**: Canlı veya gerçek walk-forward simülasyonu çalıştırılamaz (`allow_walk_forward_execution: False`), parametre optimizasyonu yapılamaz (`allow_optimizer_execution: False`), benchmark yürütülemez (`allow_benchmark_execution: False`), gerçek metrikler (Sharpe, alfa, beta, kazanma oranı) hesaplanamaz (`allow_metric_calculation: False`). Tüm yürütmeler güvenli kuru koşum yer tutucularıdır (`execution_blocked=True`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **35 NO-GO ve 10 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 35 NO-GO kuralını (canlı ticaret, broker API, doğrulama sözleşmesini sinyal sayma, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 10 SAFE-GO ilkesini denetler (`safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`READY_FOR_PHASE_148_STRESS_TESTING_HANDOFF`), walk-forward doğrulama ve OOS benchmark katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 148 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 148 (Stress Testing, Scenario Simulation and Robustness) fazına devir eksiksiz hazırlanmıştır (`phase_148_handoff.py`).
- **Çalışma Rejimi**: `advanced_walk_forward_validation_enabled=True`, `walk_forward_dry_run_default=True`, `walk_forward_local_only=True`, `walk_forward_research_only=True`, `walk_forward_allow_live_trading=False`. Mevcut faz: 147, Bir sonraki faz: 148 (Stress Testing, Scenario Simulation and Robustness), Nihai hedef: 160.

## Phase 148 Stress Testing and Scenario Simulation Safe Usage Guidelines
- **Kesin Non-Signal ve Senaryo Sözleşmesi Güvenliği**: Phase 148 Stress Testing and Scenario Simulation katmanı yalnızca çevrimdışı ve yerel araştırma senaryo sözleşmelerini, şok yer tutucularını, metrik formüllerini ve stres manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Stres Testi ve Simülasyon Yürütmesi**: Canlı veya gerçek stres testi simülasyonu çalıştırılamaz (`allow_stress_test_execution: False`), senaryo yürütülemez (`allow_scenario_simulation: False`), Monte Carlo simülasyonu çalıştırılamaz (`allow_monte_carlo_execution: False`), parametre optimizasyonu yapılamaz (`allow_optimizer_execution: False`), gerçek stres metrikleri (Stressed PnL, VaR, CVaR, MaxDD) hesaplanamaz (`allow_metric_calculation: False`). Tüm yürütmeler güvenli kuru koşum yer tutucularıdır (`execution_blocked=True`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **14 NO-GO ve 7 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 14 NO-GO kuralını (canlı ticaret, broker API, stres sözleşmesini sinyal sayma, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 7 SAFE-GO ilkesini denetler (`stress_testing_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`READY_FOR_PHASE_149_MONTE_CARLO_ROBUSTNESS_HANDOFF`), stres testi ve senaryo simülasyonu katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 149 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 149 (Monte Carlo Robustness and Parameter Stability) fazına devir eksiksiz hazırlanmıştır (`phase_149_handoff.py`).
- **Current Phase: 148, Next Phase: 149 (Monte Carlo Robustness and Parameter Stability), Nihai hedef: 160**.

## Phase 149 Monte Carlo Robustness and Parameter Stability Safe Usage Guidelines
- **Kesin Non-Signal ve Dayanıklılık Sözleşmesi Güvenliği**: Phase 149 Monte Carlo Robustness and Parameter Stability katmanı yalnızca çevrimdışı ve yerel araştırma simülasyon sözleşmelerini, bootstrap yer tutucularını, metrik formüllerini ve dayanıklılık manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Monte Carlo ve Simülasyon Yürütmesi**: Canlı veya gerçek Monte Carlo simülasyonu çalıştırılamaz (`allow_monte_carlo_execution: False`), bootstrap örneklemesi koşturulamaz (`allow_bootstrap_sampling: False`), parametre optimizasyonu yapılamaz (`allow_parameter_optimization: False`), parametre taraması yürütülemez (`allow_parameter_grid_sweep: False`), gerçek dayanıklılık metrikleri (Monte Carlo VaR, CVaR, resampled Sharpe, drawdown dağılımları) hesaplanamaz (`allow_metric_calculation: False`). Tüm yürütmeler güvenli kuru koşum yer tutucularıdır (`execution_blocked=True`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **14 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 14 NO-GO kuralını (canlı ticaret, broker API, dayanıklılık sözleşmesini sinyal sayma, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 8 SAFE-GO ilkesini denetler (`monte_carlo_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`READY_FOR_PHASE_150_BACKTEST_GOVERNANCE_HANDOFF`), Monte Carlo dayanıklılık ve parametre kararlılığı katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 150 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 150 (Backtest Governance, Bias Control and Overfitting Safeguards) fazına devir eksiksiz hazırlanmıştır (`phase_150_handoff.py`).
- **Çalışma Rejimi**: `advanced_monte_carlo_enabled=True`, `monte_carlo_dry_run_default=True`, `monte_carlo_local_only=True`, `monte_carlo_research_only=True`, `monte_carlo_allow_live_trading=False`. Mevcut faz: 149, Bir sonraki faz: 150 (Backtest Governance, Bias Control and Overfitting Safeguards), Nihai hedef: 160.

## Phase 150 Backtest Governance and Bias Control Safe Usage Guidelines
- **Kesin Non-Signal ve Yönetişim Sözleşmesi Güvenliği**: Phase 150 Backtest Governance and Bias Control katmanı yalnızca çevrimdışı ve yerel araştırma yönetişim sözleşmelerini, yanlılık kontrollerini, sonuç iddia sınırlarını, gerçekçilik denetimlerini ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Backtest, Simülasyon, Optimizasyon ve Sonuç İddiası**: Canlı veya gerçek backtest motoru çalıştırılamaz (`allow_backtest_execution: False`), simülasyon koşturulamaz (`allow_simulation_execution: False`), parametre optimizasyonu yapılamaz (`allow_optimizer_execution: False`), gerçek metrikler (Sharpe, drawdown, kârlılık vb.) hesaplanamaz (`allow_metric_calculation: False`), sonuç veya performans iddiaları sunulamaz (`allow_result_claim: False`, `allow_performance_claim: False`). Tüm yürütmeler güvenli kuru koşum yer tutucularıdır (`execution_blocked=True`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **10 NO-GO ve 3 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 10 NO-GO kuralını (canlı ticaret, broker API, yönetişim sözleşmesini sinyal sayma, backtest yürütme, sonuç/performans iddiası, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 3 SAFE-GO ilkesini denetler (`backtest_governance_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`READY_FOR_PHASE_151_BENCHMARK_EVALUATION_HANDOFF`), backtest yönetişimi ve yanlılık kontrolü katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 151 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 151 (Benchmark Comparison and Strategy Evaluation Reports) fazına devir eksiksiz hazırlanmıştır (`phase_151_handoff.py`).
- **Çalışma Rejimi**: `advanced_backtest_governance_enabled=True`, `backtest_governance_dry_run_default=True`, `backtest_governance_local_only=True`, `backtest_governance_research_only=True`, `backtest_governance_allow_live_trading=False`. Mevcut faz: 150, Bir sonraki faz: 151 (Benchmark Comparison and Strategy Evaluation Reports), Nihai hedef: 160.

## Phase 151 Benchmark Comparison and Strategy Evaluation Reports Safe Usage Guidelines
- **Kesin Non-Signal ve Değerlendirme Sözleşmesi Güvenliği**: Phase 151 Benchmark Comparison and Strategy Evaluation Reports katmanı yalnızca çevrimdışı ve yerel araştırma değerlendirme sözleşmelerini, benchmark evrenlerini, hesaplanmamış metrik yer tutucularını ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı, alfa tetiği veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`).
- **Sıfır Gerçek Benchmark Simülasyonu, Strateji Backtesti ve Metrik Hesabı**: Canlı veya gerçek benchmark simülasyonu çalıştırılamaz (`allow_benchmark_execution: False`), strateji backtesti koşturulamaz (`allow_backtest_execution: False`), parametre optimizasyonu yapılamaz (`allow_optimizer_execution: False`), gerçek metrikler (Sharpe, win-rate, alfa, beta, drawdown vb.) hesaplanamaz (`allow_metric_calculation: False`), sonuç veya performans iddiaları sunulamaz (`allow_result_claim: False`, `allow_performance_claim: False`). Tüm yürütmeler güvenli kuru koşum yer tutucularıdır (`execution_blocked=True`).
- **Sıfır Strateji Onayı ve Sermaye Tahsisi**: Otomatik strateji onayı verilemez (`allow_strategy_approval: False`), sermaye tahsisatı veya portföy oluşturulamaz (`allow_capital_allocation: False`). Tüm çıktılar yalnızca ampirik araştırma hipotezi niteliğindedir.
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **15 NO-GO ve 7 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 15 NO-GO kuralını (canlı ticaret, broker API, değerlendirme sözleşmesini sinyal sayma, benchmark simülasyonu koşturma, sonuç/performans iddiası, strateji onayı verme, model eğitme, çıkarım yapma, hazırlık puanını üretim onayı sayma, haber metni çekme vb.) ve 7 SAFE-GO ilkesini denetler (`benchmark_evaluation_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.00 hazırlık skoru (`benchmark_evaluation_contract_ready_non_production`), değerlendirme sözleşmeleri ve benchmark karşılaştırma katmanının sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı işlem yetkilendirmesi değildir.
- **Phase 152 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 152 (Backtest Acceptance Report) fazına devir eksiksiz hazırlanmıştır (`phase_152_handoff.py`).
- **Çalışma Rejimi**: `advanced_benchmark_evaluation_enabled=True`, `benchmark_evaluation_dry_run_default=True`, `benchmark_evaluation_local_only=True`, `benchmark_evaluation_research_only=True`, `benchmark_evaluation_allow_live_trading=False`. Mevcut faz: 151, Bir sonraki faz: 152 (Backtest Acceptance Report), Nihai hedef: 160.

## Phase 152 Backtest Acceptance Report Safe Usage Guidelines
- **Kesin Non-Signal ve Kabul Sözleşmesi Güvenliği**: Phase 152 Backtest Acceptance Report katmanı yalnızca çevrimdışı ve yerel araştırma kabul sözleşmelerini, bileşen kayıtlarını, kontrol noktalarını ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya getiri beklentisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Gerçek Backtest, Benchmark Simülasyonu ve Metrik Hesabı**: Canlı veya gerçek backtest motoru çalıştırılamaz (`allow_backtest_execution: False`), benchmark simülasyonu koşturulamaz (`allow_benchmark_execution: False`), parametre optimizasyonu yapılamaz (`allow_optimizer_execution: False`), gerçek metrikler hesaplanamaz (`allow_metric_calculation: False`), sonuç veya performans iddiaları sunulamaz (`allow_result_claim: False`, `allow_performance_claim: False`).
- **Sıfır Strateji Onayı, Sermaye Tahsisi ve Portföy Yürütmesi**: Otomatik strateji onayı verilemez (`allow_strategy_approval: False`), sermaye tahsisatı veya portföy yapısı oluşturulamaz (`allow_capital_allocation: False`, `allow_portfolio_construction: False`, `allow_position_sizing: False`).
- **Otomatik Aksiyon Yasakları**: Bulgular sistem seviyesinde denetlenir; `auto-run backtest`, `auto-approve strategy`, `auto-allocate capital` gibi eylemler yasaklanmıştır.
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **20 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 20 NO-GO kuralını ve 8 SAFE-GO ilkesini denetler (`backtest_acceptance_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`backtest_acceptance_contract_ready_non_production`), backtest kabul bloğunun sözleşmesel ve mimari eksiksizliğini belgeler; asla kârlılık, getiri güvencesi, strateji onayı veya canlı işlem yetkilendirmesi değildir.
- **Phase 153 Handoff Güvencesi**: 13 yapılandırılmış devir maddesi ile Phase 153 (Portfolio Construction, Position Sizing and Risk Budgeting) fazına devir eksiksiz hazırlanmıştır (`phase_153_handoff.py`).
- **Çalışma Rejimi**: `advanced_backtest_acceptance_enabled=True`, `backtest_acceptance_dry_run_default=True`, `backtest_acceptance_local_only=True`, `backtest_acceptance_research_only=True`, `backtest_acceptance_allow_live_trading=False`. Mevcut faz: 152, Bir sonraki faz: 153 (Portfolio Construction, Position Sizing and Risk Budgeting), Nihai hedef: 160.

## Phase 153 Portfolio Construction, Position Sizing and Risk Budgeting Safe Usage Guidelines
- **Kesin Non-Signal ve Portföy Sözleşmesi Güvenliği**: Phase 153 Portfolio Construction katmanı yalnızca çevrimdışı ve yerel araştırma portföy sözleşmelerini, boyutlandırma şablonlarını, risk bütçeleme yapılarını ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`, `allow_real_order: False`).
- **Sıfır Gerçek Optimizasyon ve Portföy Ağırlıklandırması**: Sayısal portföy optimizasyonu çalıştırılamaz (`allow_real_optimization: False`), gerçek portföy ağırlıkları veya sermaye tahsisatı üretilemez (`allow_real_portfolio_weights: False`, `actual_weight=None`).
- **Sıfır Gerçek Pozisyon Boyutlandırma ve Bütçe Tahsisi**: Gerçek lot veya kontrat büyüklüğü hesaplanamaz (`allow_real_lot_sizing: False`, `actual_size_calculated=None`, `actual_lot_generated=None`), gerçek risk bütçesi tahsisi yapılamaz (`allow_real_capital_allocation: False`, `actual_budget_calculated=None`).
- **Sıfır Gerçek Portföy Metriği Hesabı**: Gerçek Sharpe, Sortino, VaR, ES veya MaxDD hesaplanamaz (`allow_metric_calculation: False`, `is_calculated=False`, `actual_value=None`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **20 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 20 NO-GO kuralını ve 8 SAFE-GO ilkesini denetler (`portfolio_construction_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`portfolio_construction_contract_ready_non_production`), portföy inşa sözleşmelerinin ve yönetişim altyapısının eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya portföy onayı değildir.
- **Phase 154 Handoff Güvencesi**: 14 yapılandırılmış devir maddesi ile Phase 154 (Portfolio Optimization Contracts) fazına devir eksiksiz hazırlanmıştır (`phase_154_handoff.py`).
- **Çalışma Rejimi**: `advanced_portfolio_construction_enabled=True`, `portfolio_construction_dry_run_default=True`, `portfolio_construction_local_only=True`, `portfolio_construction_research_only=True`, `portfolio_construction_allow_live_trading=False`. Mevcut faz: 153, Bir sonraki faz: 154 (Portfolio Optimization Contracts), Nihai hedef: 160.

## Phase 154 Portfolio Optimization and Allocation Constraints Safe Usage Guidelines
- **Kesin Non-Signal ve Portföy Optimizasyon Sözleşmesi Güvenliği**: Phase 154 Portfolio Optimization katmanı yalnızca çevrimdışı ve yerel araştırma amaç fonksiyonlarını, tahsis kısıtlarını, çözücü arayüzlerini ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_execution: False`, `allow_real_order: False`).
- **Sıfır Gerçek Optimizasyon Çözücü Yürütmesi**: Sayısal optimizasyon kütüphaneleri (CVXPY, SciPy, Gurobi, MOSEK vb.) koşturulamaz (`allow_real_optimization: False`, `allows_execution: False`).
- **Sıfır Gerçek Optimal Portföy Ağırlıkları ve Sermaye Tahsisi**: Gerçek optimal portföy ağırlıkları hesaplanamaz (`allow_real_portfolio_weights: False`, `actual_weight=None`), gerçek sermaye tahsisatı üretilemez (`allow_real_capital_allocation: False`, `actual_allocated_capital=None`).
- **Sıfır Yeniden Dengeleme (Rebalance) Emri**: Portföy yeniden dengeleme emirleri ve işlem biletleri üretilemez (`allow_real_rebalance: False`, `rebalance_order_generated=False`).
- **Sıfır Gerçek Optimizasyon Metriği Hesabı**: Gerçek optimizasyon metrikleri (Sharpe, Diversification Ratio, Tracking Error vb.) hesaplanamaz (`allow_metric_calculation: False`, `is_calculated=False`, `actual_value=None`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **18 NO-GO ve 8 SAFE-GO Güvenlik Sınırı (SECURE)**: Sistem her çalıştırmada 18 NO-GO kuralını ve 8 SAFE-GO ilkesini denetler (`portfolio_optimization_safety_boundary.py`). Tüm denetimler SECURE olmak zorundadır.
- **52 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir ve kârlılık iddialarını temsil eden 52 kolon karantina politikasındadır (`optimization_forbidden_column_policies.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`portfolio_optimization_contract_ready_non_production`), optimizasyon sözleşmelerinin ve yönetişim altyapısının eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya portföy onayı değildir.
- **Phase 155 Handoff Güvencesi**: 15 yapılandırılmış devir maddesi ile Phase 155 (Portfolio Risk Attribution and Reporting) fazına devir eksiksiz hazırlanmıştır (`phase_155_handoff.py`).
- **Çalışma Rejimi**: `advanced_portfolio_optimization_enabled=True`, `portfolio_optimization_dry_run_default=True`, `portfolio_optimization_local_only=True`, `portfolio_optimization_research_only=True`, `portfolio_optimization_allow_live_trading=False`. Mevcut faz: 154, Bir sonraki faz: 155 (Portfolio Risk Attribution and Reporting), Nihai hedef: 160.

## Phase 155 Risk Reporting, Exposure Attribution and Limit Monitoring Safe Usage Guidelines
- **Kesin Non-Signal ve Risk Rapor Sözleşmesi Güvenliği**: Phase 155 Risk Reporting katmanı yalnızca çevrimdışı ve yerel araştırma risk rapor sözleşmelerini, maruziyet ayrıştırma şablonlarını, limit izleme kurallarını ve bütünlük manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Gerçek Risk Raporu Yürütmesi**: Gerçek risk raporları derlenemez ve çalıştırılamaz (`allow_risk_report_execution: False`, `risk_report_generated: False`).
- **Sıfır Gerçek Maruziyet (Exposure) Hesabı**: Gerçek portföy maruziyeti ve maruziyet katkıları hesaplanamaz (`allow_exposure_attribution_execution: False`, `actual_exposure_calculated=None`).
- **Sıfır Canlı Limit İzleme ve Alarm Tetikleme**: Gerçek limit izleme döngüleri ve canlı alarm tetiklemeleri koşturulamaz (`allow_limit_monitoring_execution: False`, `limit_monitoring_executed: False`). Dış bildirimler (webhook, SMS, e-posta) tamamen devre dışıdır (`alert_routing_disabled: True`).
- **Sıfır Dashboard Üretimi**: Kullanıcı veya operatör için canlı/interaktif web gösterge paneli (dashboard) render edilmez (`dashboard_generated: False`).
- **Sıfır Otomatik Portföy Düzeltmesi**: Limit aşımlarında veya risk uyarılarında otomatik rebalance, lot küçültme veya pozisyon kapatma yapılmaz (`allow_portfolio_adjustment: False`, `portfolio_adjustment_generated: False`).
- **Sıfır Gerçek Risk Metriği Hesabı**: Gerçek VaR, Beklenen Kayıp (ES), volatilite, maksimum düşüş hesaplanamaz (`allow_metric_calculation: False`, `allow_var_calculation: False`, `actual_value=None`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **25 NO-GO ve 21 SAFE-GO Güvenlik Sınırı (ACTIVE)**: Sistem her çalıştırmada 25 NO-GO kuralını ve 21 SAFE-GO ilkesini denetler (`risk_reporting_safety_boundary.py`). Tüm denetimler SAFETY_BOUNDARY_ACTIVE olmak zorundadır.
- **48 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir, alarm ve risk iddiası kolonları karantina politikasındadır (`risk_reporting_forbidden_column_policies.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`risk_reporting_contract_ready_non_production`), risk rapor sözleşmelerinin ve yönetişim altyapısının eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya portföy onayı değildir.
- **Phase 156 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 156 (Portfolio Scenario Testing and Drawdown Control) fazına devir eksiksiz hazırlanmıştır (`phase_156_handoff.py`).
- **Çalışma Rejimi**: `advanced_risk_reporting_enabled=True`, `risk_reporting_dry_run_default=True`, `risk_reporting_local_only=True`, `risk_reporting_research_only=True`, `risk_reporting_allow_live_trading=False`. Mevcut faz: 155, Bir sonraki faz: 156 (Portfolio Scenario Testing and Drawdown Control), Nihai hedef: 160.

## Phase 156 Portfolio Scenario Testing and Drawdown Control Safe Usage Guidelines
- **Kesin Non-Signal ve Senaryo-Kontrol Sözleşmesi Güvenliği**: Phase 156 Portfolio Scenario Testing and Drawdown Control katmanı yalnızca çevrimdışı ve yerel araştırma senaryo test sözleşmelerini, şok modellerini, drawdown kontrol şablonlarını, toparlanma planlarını ve kontrol aksiyon yer tutucularını tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Gerçek Senaryo Testi ve PnL Hesabı**: Gerçek senaryo simülasyonları koşturulamaz ve portföy kriz kaybı hesaplanamaz (`allow_scenario_execution: False`, `scenario_tested: False`).
- **Sıfır Gerçek Drawdown Kontrolü ve İhlal Tetiklemesi**: Gerçek drawdown izleme döngüleri ve canlı ihlal tetiklemeleri koşturulamaz (`allow_drawdown_control_execution: False`, `drawdown_controlled: False`).
- **Sıfır Otomatik Portföy Müdahalesi, Hedge ve De-risking**: Risk veya kriz durumlarında otomatik pozisyon kapatma, koruma (hedge), risk düşürme (de-risking), yeniden dengeleme veya portföy dondurma yapılmaz (`allow_portfolio_adjustment: False`, `allow_hedge_execution: False`, `allow_derisk_execution: False`).
- **Sıfır Gerçek Metrik Hesabı**: Gerçek senaryo kaybı, maksimum drawdown, toparlanma süresi veya dayanıklılık skoru hesaplanamaz (`allow_metric_calculation: False`, `actual_value=None`).
- **Sıfır Dış Bildirim ve Dashboard Üretimi**: Dış bildirimler (webhook, SMS, e-posta) tamamen devre dışıdır (`alert_routing_disabled: True`). Canlı/interaktif gösterge paneli (dashboard) render edilmez (`dashboard_generated: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **28 NO-GO ve 24 SAFE-GO Güvenlik Sınırı (ACTIVE)**: Sistem her çalıştırmada 28 NO-GO kuralını ve 24 SAFE-GO ilkesini denetler (`portfolio_scenario_control_safety_boundary.py`). Tüm denetimler SAFETY_BOUNDARY_ACTIVE olmak zorundadır.
- **52 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir, alarm ve kontrol aksiyonu kolonları karantina politikasındadır (`portfolio_scenario_forbidden_column_policies.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`portfolio_scenario_control_contract_ready_non_production`), senaryo ve kontrol sözleşmelerinin eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya portföy onayı değildir.
- **Phase 157 Handoff Güvencesi**: 10 yapılandırılmış devir maddesi ile Phase 157 (Portfolio Acceptance Report) fazına devir eksiksiz hazırlanmıştır (`phase_157_handoff.py`).
- **Çalışma Rejimi**: `advanced_portfolio_scenario_control_enabled=True`, `portfolio_scenario_control_dry_run_default=True`, `portfolio_scenario_control_local_only=True`, `portfolio_scenario_control_research_only=True`, `portfolio_scenario_control_allow_live_trading=False`. Mevcut faz: 156, Bir sonraki faz: 157 (Portfolio Acceptance Report), Nihai hedef: 160.

## Phase 157 Portfolio Acceptance Report and Phase 153-157 Consolidated Acceptance Safe Usage Guidelines
- **Kesin Non-Signal ve Portföy Kabul Sözleşmesi Güvenliği**: Phase 157 Portfolio Acceptance Report katmanı yalnızca çevrimdışı ve yerel araştırma kabul profillerini, bileşen checkpoint sözleşmelerini, Phase 153-156 faz kabul denetimlerini, bağımlılık ve doğrulama kanıtlarını, go/no-go sınırlarını, blocker/gap/warning/finding kayıtlarını ve master kabul manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Gerçek Portföy İnşası ve Pozisyon Büyüklüğü Hesabı**: Gerçek portföy varlıkları seçilemez, sermaye tahsis edilemez ve pozisyon büyüklüğü hesaplanamaz (`allow_portfolio_construction: False`, `allow_position_sizing: False`).
- **Sıfır Gerçek Optimizasyon ve Kısıt Çözümü**: Gerçek matematiksel optimizasyon çözücüsü koşturulamaz ve yeniden dengeleme yapılamaz (`allow_portfolio_optimization: False`, `allow_rebalance_execution: False`).
- **Sıfır Gerçek Risk Raporlaması ve Senaryo Koşumu**: Gerçek risk raporu derlenemez, gerçek maruziyet hesaplanamaz, stres testi simülasyonu koşturulamaz ve gerçek drawdown kontrol döngüsü çalıştırılamaz (`allow_risk_reporting: False`, `allow_scenario_execution: False`, `allow_drawdown_control: False`).
- **Sıfır Gerçek Metrik ve Performans Hesabı**: Gerçek getiri, Sharpe, VaR, MaxDD, kabul performans skoru hesaplanamaz (`allow_metric_calculation: False`, `actual_value=None`).
- **Sıfır Dış Bildirim ve Dashboard Üretimi**: Dış bildirimler (webhook, SMS, e-posta) tamamen devre dışıdır (`alert_routing_disabled: True`). Canlı/interaktif gösterge paneli (dashboard) render edilmez (`dashboard_generated: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **32 NO-GO ve 28 SAFE-GO Güvenlik Sınırı (ACTIVE)**: Sistem her çalıştırmada 32 NO-GO kuralını ve 28 SAFE-GO ilkesini denetler (`portfolio_acceptance_safety_boundary.py`). Tüm denetimler SAFETY_BOUNDARY_ACTIVE olmak zorundadır.
- **56 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir, onay iddiası, gerçek metrik ve kontrol aksiyonu kolonları karantina politikasındadır (`portfolio_acceptance_models.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`portfolio_acceptance_contract_ready_non_production`), portföy kabul sözleşmelerinin ve checkpoint altyapısının eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlı portföy onayı değildir.
- **Phase 158 Handoff Güvencesi**: 12 yapılandırılmış devir maddesi ile Phase 158 fazına devir eksiksiz hazırlanmıştır (`phase_158_handoff.py`).
- **Çalışma Rejimi**: `advanced_portfolio_acceptance_enabled=True`, `portfolio_acceptance_dry_run_default=True`, `portfolio_acceptance_local_only=True`, `portfolio_acceptance_research_only=True`, `portfolio_acceptance_allow_live_trading=False`. Mevcut faz: 157, Bir sonraki faz: 158, Nihai hedef: 160.

## Phase 158 Full-System Integration and Advanced Acceptance Rehearsal Safe Usage Guidelines
- **Kesin Non-Signal ve Entegrasyon Sözleşmesi Güvenliği**: Phase 158 Full-System Integration katmanı yalnızca çevrimdışı ve yerel araştırma entegrasyon sözleşmelerini, kabul provası (acceptance rehearsal) şablonlarını, bileşen checkpoint sözleşmelerini, sistem bağımlılık ve doğrulama kanıtlarını, güvenlik sınırlarını ve master entegrasyon manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Gerçek Portföy İnşası ve Pozisyon Büyüklüğü Hesabı**: Gerçek portföy varlıkları seçilemez, sermaye tahsis edilemez ve pozisyon büyüklüğü hesaplanamaz (`allow_portfolio_construction: False`, `allow_position_sizing: False`).
- **Sıfır Gerçek Optimizasyon ve Kısıt Çözümü**: Gerçek matematiksel optimizasyon çözücüsü koşturulamaz ve yeniden dengeleme yapılamaz (`allow_portfolio_optimization: False`, `allow_rebalance_execution: False`).
- **Sıfır Gerçek Risk Raporlaması ve Senaryo Koşumu**: Gerçek risk raporu derlenemez, gerçek maruziyet hesaplanamaz, stres testi simülasyonu koşturulamaz ve gerçek drawdown kontrol döngüsü çalıştırılamaz (`allow_risk_reporting: False`, `allow_scenario_execution: False`, `allow_drawdown_control: False`).
- **Sıfır Gerçek Kabul Provası Yürütmesi**: Gerçek kabul provası ve canlı sistem tatbikatı koşturulamaz (`allow_rehearsal_execution: False`, `rehearsal_executed: False`).
- **Sıfır Gerçek Metrik ve Performans Hesabı**: Gerçek getiri, Sharpe, VaR, MaxDD, entegrasyon performans skoru hesaplanamaz (`allow_metric_calculation: False`, `actual_value=None`).
- **Sıfır Dış Bildirim ve Dashboard Üretimi**: Dış bildirimler (webhook, SMS, e-posta) tamamen devre dışıdır (`alert_routing_disabled: True`). Canlı/interaktif gösterge paneli (dashboard) render edilmez (`dashboard_generated: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **18 NO-GO, 6 Non-Production, 5 Dry-Run ve 10 Review Gate Güvenlik Sınırı (SECURE/PASS)**: Sistem her çalıştırmada 18 NO-GO kuralını, 6 non-production kuralını, 5 dry-run kuralını ve 10 inceleme kapısını denetler (`full_system_integration_safety_boundary.py`). Tüm denetimler PASS/SECURE olmak zorundadır.
- **52 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir, onay iddiası, gerçek metrik ve tatbikat/rehearsal kolonları karantina politikasındadır (`full_system_integration_models.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`full_system_integration_contract_ready_non_production`), tam sistem entegrasyon sözleşmelerinin ve kabul provası altyapısının eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlıya geçiş onayı değildir.
- **Phase 159 Handoff Güvencesi**: 13 yapılandırılmış devir maddesi ile Phase 159 (Final Hardening, Operator Runbook and Release Candidate) fazına devir eksiksiz hazırlanmıştır (`phase_159_handoff.py`).
- **Çalışma Rejimi**: `advanced_full_system_integration_enabled=True`, `full_system_integration_dry_run_default=True`, `full_system_integration_local_only=True`, `full_system_integration_research_only=True`, `full_system_integration_allow_live_trading=False`. Mevcut faz: 158, Bir sonraki faz: 159 (Final Hardening, Operator Runbook and Release Candidate), Nihai hedef: 160.

## Phase 159 Final Hardening, Operator Runbook and Release Candidate Safe Usage Guidelines
- **Kesin Non-Signal ve Hardening Sözleşmesi Güvenliği**: Phase 159 Final Hardening katmanı yalnızca yerel ve çevrimdışı final hardening sözleşmelerini, operatör prosedürlerini (runbooks), release candidate checklist'lerini, dondurma (freeze) denetimlerini, sistem envanterlerini ve release candidate manifestolarını tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Üretim Ortamı ve Release Yayınlama**: Gerçek ortama veya sunucuya deployment, production release yayını veya harici paket yüklemesi yapılamaz (`allow_production_deployment: False`, `release_published: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **Sıfır Gerçek Backtest ve Portföy Optimizasyonu**: Gerçek backtest motoru ve portföy optimizasyon çözücüsü çalıştırılamaz (`allow_backtest_execution: False`, `allow_optimizer_execution: False`).
- **Sıfır Gerçek Olay Müdahalesi ve Kurtarma**: Canlı sistem veya süreçler üzerinde otomatik incident response ve restart yapılamaz; sadece offline sözleşme ve kılavuzlar işletilir (`allow_real_incident_response: False`, `allow_real_recovery: False`).
- **10 NO-GO, 8 Safe-Go ve 10 Manuel İnceleme Kapısı (PASS/SECURE)**: Sistem her çalıştırmada 10 NO-GO kuralını, 8 Safe-Go kuralını ve 10 operatör inceleme kapısını denetler (`final_hardening_safety_boundary.py`). Tüm denetimler PASS/SECURE olmak zorundadır.
- **50 Yasaklı Kolon Karantinası**: Canlı işlem, sızıntı, broker emir, deployment, onay iddiası, gerçek model ve icra kolonları karantina politikasındadır (`final_hardening_models.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: 1.0000 hazırlık skoru (`final_hardening_contract_ready_non_production`), nihai hardening ve operatör rehber sözleşmelerinin eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlıya geçiş onayı değildir.
- **Çalışma Rejimi**: `advanced_final_hardening_enabled=True`, `final_hardening_dry_run_default=True`, `final_hardening_local_only=True`, `final_hardening_research_only=True`, `final_hardening_allow_live_trading=False`. Mevcut faz: 159, Bir sonraki faz: 160 (Full Advanced Bot Final Delivery), Nihai hedef: 160.

## Phase 160 Full Advanced Bot Final Delivery Safe Usage Guidelines
- **Kesin Non-Signal ve Final Delivery Sözleşmesi Güvenliği**: Phase 160 Final Delivery katmanı yalnızca yerel ve çevrimdışı nihai teslimat sözleşmelerini, sistem envanterlerini, kabul kanıtlarını, blok özetlerini, güvenlik sınırlarını ve kapanış manifestosunu tanımlar. Hiçbir koşulda AL/SAT sinyali, alım-satım tavsiyesi, emir, broker talimatı veya sermaye tahsisi içermez (`non_signal: True`, `allow_signal_generation: False`).
- **Sıfır Canlı Borsa ve Broker Emir İletimi**: Canlı borsa bağlantısı, broker API entegrasyonu, gerçek emir iletimi veya hesap yetkilendirmesi kesinlikle yapılmaz (`allow_live_trading: False`, `allow_broker_integration: False`, `allow_real_order: False`).
- **Sıfır Üretim Ortamı ve Deployment**: Gerçek ortama veya sunucuya deployment, production release yayını veya harici paket yüklemesi yapılamaz (`allow_production_deployment: False`).
- **Sıfır Model Eğitimi, Tahmin ve Serileştirme**: Gerçek model eğitimi (`fit`, `train`), tahmin yürütme (`predict`, `inference`), hedef etiket türetimi veya model ağırlığı kalıcılığı yasaktır.
- **Sıfır Gerçek Backtest ve Portföy Optimizasyonu**: Gerçek backtest motoru ve portföy optimizasyon çözücüsü çalıştırılamaz (`allow_backtest_execution: False`, `allow_portfolio_execution: False`).
- **16 NO-GO ve 16 Safe-Go Güvenlik Sınırı (PASS/SECURE)**: Sistem her çalıştırmada 16 NO-GO kuralını ve 16 Safe-Go kuralını denetler (`final_delivery_safety_boundary.py`). Tüm denetimler PASS/SECURE olmak zorundadır.
- **Yasaklı Kolon Politikaları**: Sızıntı, gelecek tahmini veya canlı emir içeren tüm kolonlar sistem seviyesinde karantina altındadır (`final_delivery_forbidden_column_policies.py`).
- **Hazırlık Skoru Asla Finansal Onay veya Canlıya Geçiş Onayı Değildir**: %100.0 hazırlık skoru (`FULL_ADVANCED_BOT_FINAL_DELIVERY_READY`), nihai teslimat sözleşmelerinin eksiksizliğini belgeler; asla kârlılık, getiri güvencesi veya canlıya geçiş onayı değildir.
- **160-Fazlık Plan Kapanışı**:
  - `current_phase = 160`
  - `target_final_phase = 160`
  - `next_phase = None`
  - `phase_160_completed = True`
  - `final_plan_closed = True`
- **Çalışma Rejimi**: `advanced_final_delivery_enabled=True`, `final_delivery_dry_run_default=True`, `final_delivery_local_only=True`, `final_delivery_research_only=True`, `final_delivery_allow_live_trading=False`. Mevcut faz: 160, Sonraki faz: Yok (Plan Kapandı).
